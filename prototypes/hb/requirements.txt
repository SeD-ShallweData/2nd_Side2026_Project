# 돈워리 웹앱 실행에 필요한 패키지.
# 개발 서버에서 실제로 동작 중인 버전을 그대로 고정했다.
#
#   python3 -m pip install -r requirements.txt
#
# 주의: sentence-transformers가 처음 실행될 때 임베딩 모델 BAAI/bge-m3(약 2.2GB)를
#       Hugging Face에서 내려받는다. 오프라인 환경이라면 미리 캐시를 받아두고
#       HF_HOME 환경변수로 캐시 경로를 지정할 것.

flask==3.1.3
chromadb==1.5.9
sentence-transformers==5.6.1
openai==2.48.0
python-dotenv==1.2.2
requests==2.34.2
