# Mock가 기본값입니다. 실제 어댑터가 준비된 기능만 real로 전환하세요.
APP_DATA_MODE=mock

# real 공급자 장애 시 기능별 Mock 전환을 허용합니다. 응답에는 mocked 상태가 남습니다.
ENABLE_MOCK_FALLBACK=false

# 실제 모드에서는 읽기 전용 PostgreSQL 계정 URL만 사용합니다.
# DB 서버에서 부여한 값을 .env.local에 넣고 저장소에는 커밋하지 않습니다.
DATABASE_URL=postgresql://READ_ONLY_USER:CHANGE_ME@127.0.0.1:5433/wageguard
DB_SSL=false

# 내부 서비스 주소입니다. 브라우저는 이 주소를 직접 호출하지 않습니다.
RAG_API_URL=http://127.0.0.1:5050
RAG_TIMEOUT_MS=20000
CONTRACT_ANALYSIS_URL=http://127.0.0.1:5000
CONTRACT_TIMEOUT_MS=90000

# Mock UI의 짧은 loading 상태를 확인하기 위한 지연 시간입니다.
MOCK_DELAY_MS=250

# 상담은 Mock DB 컨텍스트를 사용하더라도 두 실제 LLM API를 동시에 호출합니다.
SHARED_API_KEY_FILE=/data/shared-SeD/api_key.env
UPSTAGE_API_URL=https://api.upstage.ai/v1/chat/completions
UPSTAGE_MODEL=solar-pro3
SKT_API_URL=https://awf-gw.adot.ai/v1/chat/completions
SKT_MODEL=A.X-K1
LLM_TIMEOUT_MS=45000

# 질문·답변 원문은 저장하지 않고 선택 결과와 성능 지표만 저장합니다.
SAVE_COMPARISON_FEEDBACK=true
