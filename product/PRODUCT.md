# 통합 제품 기준 정보

- 초기 기준 프로토타입: `jcu_branch`
- 기준 커밋: `69f781e`
- 초기화 시점: 2026-08-10

현재 `product/`는 `jcu_branch`의 Next.js 통합 프로토타입을 그대로 기준본으로 가져온 상태입니다.

팀원별 원본은 `prototypes/`에 보존하며, 앞으로 채택한 RAG·DB·프런트엔드·계약서 기능은 최신 `main`에서 만든 기능 브랜치를 통해 이 디렉터리에 통합합니다.

## 개발 원칙

- 개인 프로토타입 원본은 직접 수정하지 않습니다.
- 실제 제품 변경은 `product/`에서 진행합니다.
- ML 운영 DB와 노동법 RAG DB의 저장소와 접근 권한을 분리합니다.
- 기능별 검증 후 Pull Request로 `main`에 반영합니다.
