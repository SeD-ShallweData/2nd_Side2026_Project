# 후속 07 — 통합 인수 및 단계 13 판정

기준일: 2026-09-21. 소스·모델·DB·브라우저·운영 증거를 별도로 판정한다.

## 판정

**단계 13 전체 완료 아님.** 실제 PG16·로그인 연속 대화·Chrome 검증까지 범위를 넓혔으나, 새로운 법률 적용 실패, 출처 적합성/후속 질문 구체성, Linux DB CI 잔여, 팀 정책 및 운영 적용이 남아 있다. 자동 계약 PASS나 검색 matched를 법률 정확성/출시 승인으로 취급하지 않는다. MAS 설계·도입은 시작하지 않았다.

## 1. 시작 상태와 선행 잔여

- 실제 작업 경로: `C:\Users\jcu03\Desktop\2nd_Side2026_Project`.
- `AGENTS.md`, `product/AGENTS.md`, 후속 01~06, migration/worker 지침을 확인했다. 시작 worktree clean.
- HEAD `38f235208138379ff6f16f6b62d0bfc0b8a1f962`, `task/reply-prompt-upgrade`와 그 upstream 일치.
- `origin/main` 및 읽기 전용 `git ls-remote origin refs/heads/main`: `4b9eae4ab61613f06bd5cd5b1a1bd760c15ae3be`.
- main은 HEAD의 조상, `git rev-list --left-right --count origin/main...HEAD` = `0 7`. 작업 6.5 충돌 해결 커밋에 관측 시점 최신 main이 실제 포함돼 있다. 원격이 이후 움직이지 않는다는 보장은 아니다.
- 처음부터 남아 있던 항목: 브라우저 인수, 실제 provider+로그인 영속 상담 결합, 공개 API 정책, 원문/외부 전송 동의·보존/삭제 정책, crash-pending chat request UX, 운영 migration/role/worker 적용.
- Git write·PR·운영 접속/변경·팀 메시지 전송 없음. 원본 QA credential 파일을 다시 열지 않았다.

## 2. 검증 환경과 안전 경계

| 구분 | 이번 환경 |
| --- | --- |
| DB | 후속06에서 보존한 `mw-followup06-1789983829982-e8d9a9` / 동명 `-data` volume |
| 식별 | label `moneyworry.acceptance=followup06`, loopback `127.0.0.1:55436`, DB `mw_followup06`, PG `160015` |
| 이미지 | `postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685` |
| 앱 | Node 22.23.2 / Next 16.3.0 dev, `127.0.0.1:3116`; auth/conversation Real, 회사 등 Mock |
| 검색 | localhost:5051, BGE-M3 고정 revision, 봉인 검증된 583 chunk 복사본; threshold 0.42/strong 0.30 유지 |
| 모델 | 실제 Upstage Solar; localhost:5053 투명 계측 중계에서 요청 본문/인증값 없이 호출 수·지연·usage만 기록 |
| 브라우저 | 연결된 실제 Chrome의 별도 로컬 검사 탭. 기존 GitHub/Drive 탭은 변경하지 않음 |

`db/scripts/run-followup07-local.mjs`는 위 container/image/label/port/volume/DB major를 확인하고 기존 ledger/schema를 읽기 전용 비교한다. 기존 DB 초기화·migration 재적용·복원은 하지 않는다. 기존 `wg_auth`/`wg_conversation` 비밀번호 대신 동일 role SQL로 별도 무작위 이름의 검사 역할을 생성한다. 세션/비밀번호/내부 RAG 토큰은 프로세스 메모리만 사용하며 argv·문서·fixture·로그에 쓰지 않는다.

회사 Mock DTO를 Real conversation FK와 결합하려면 회사 식별 행이 필요했다. 비어 있음을 확인한 뒤 `COMPANY_DEMO_008`(한빛테크), `UNKNOWN_SAFETY_001`(푸른건설)의 합성 FK 행 2개만 격리 DB에 추가했다. 이는 실제 회사 DB 검증이 아니다. 처음 A 전송은 해당 행이 없어 저장되지 않았고, 준비 후 별도 재검증했다.

## 3. 이번에 찾은 결함 및 수정

### 수정: 정정 발화를 옛 사실 회상으로 오인

실제 7턴 연속 평가에서 3번 `정정한다. 급여일은 10일이 아니라 15일이고 아직 미지급이다.`에 **10일**을 답했다. 기존 RECALL 정규식의 `정정한`이 `정정한다`에도 맞았고, 요청 이전 서버 이력만 보았다. 4·6·7번은 15일로 회상하므로 이 결함은 저장/요약 손실과 별개다.

- 회상 표현과 현재 정정 진술을 분리했다.
- 현재 사용자 입력에서 기존 제한된 fact extractor로 확인한 정정만 현재 사실로 덧붙여 답한다. 과거 원문·순서·회사 scope는 보존하며 임의 원문이나 assistant 조언을 사실로 올리지 않는다.
- 현재 정정/표현 변형 3개, Dual/Responses 공통 전달 회귀를 검사했다.
- 연속 평가 3번에 `15일`/옛 값 금지 계약을 추가했다.

### 수정: 회상 성공을 검색 실패처럼 표시

실제 화면은 정확한 15일/다음 주 답변 위에 `공식 근거 없음 · 정책 응답`, `찾지 못해 … 추가 확인`을 표시했다. `recall_mode`를 실행 라벨에 전달해 `사용자 진술 정리`/`대화 내용 확인 필요`, `사용자 진술 기반 · 법령 검색 미사용`으로 구분했다. 실제 생성·긴급·법률 검색 라벨과 모델 카드는 유지했다. 회상 원문과 UI 라벨은 새로고침 후 복원에서도 확인했다.

### 미수정 차단 결함: 지급 약속일과 제36조의 14일 혼동

실제 로그인 브라우저의 두 번째 질문에서 다음 오답이 나왔다.

> 회사가 약속한 지급 예정일을 기준으로 14일 이내에 입금되지 않으면 근로기준법 제36조에 따라 금품 청산 의무가 발생하므로 …

선행 입력은 급여일/이번 달 미지급/계약서·통장 보유이며 **퇴직·사망 진술은 없다**. 표시 근거는 제48·17·36조, 생성 결과는 `API 응답`이었다. [현행 제36조](https://www.law.go.kr/LSW/lsSideInfoP.do?docCls=jo&joBrNo=00&joNo=0036&lsiSeq=283457&urlMode=lsScJoRltInfoR)는 사망·퇴직이라는 적용 조건과 그 지급 사유 발생일을 규정한다. 회사 약속일로 기산점을 바꿀 수 없다.

`AQ30-payment-promise-not-termination`을 **development**로 등록하고 연속 2번의 계약도 보강했다. 실제 관측 문구가 평가기에서 FAIL임을 무호출 회귀로 확인했다. AQ30은 최소 선행 이력을 고정한 개발 사례이며 브라우저 전체 이력의 완전한 복제는 아니다. 이 등록은 생성 경로 수정이나 오답 해소를 뜻하지 않는다. 이미 수행한 동일 핵심 입력의 호출 범위를 넘겨 모델을 반복하지 않았다.

### 기타 답변 품질 잔여

- 첫 질문의 `월급을 받지 못했다`는 Product의 미지급 판별에 포함되지만 Python `_is_wage_arrears_query`의 신호에는 `받지 못`이 없다. 실제 브라우저에서는 절차를 안내하면서 표시 출처는 제37조 1개뿐이었다. `matched`라도 정기 지급·진정 안내의 출처 적합성은 별도 FAIL/보완 대상이다.
- 연속 2번의 다른 실행은 일반 자료/1350 fallback으로 안전했지만, 회사 문자에서 무엇을 기록할지 충분히 답하지 못했다. 강화된 계약으로 저장 응답을 무호출 재평가한 결과 FAIL(문자/메시지와 금액/지급일/예정일 안내 누락)이었다.
- 회사 긍정 신호의 체불 부재/안전 인증 단정과 내부 rank label 노출은 기존 계약상 방지됐으나, AQ21의 산업안전 신호 설명은 실제 공개 카드의 `안전 신호 미확인`/지역·업종 제한과 별도로 재검토한다. 개별 안전 인증이나 전체 DB 유출로 확대 해석하지 않는다.

## 4. 실행 결과

### 소스/Mock/검색

- 시작 HEAD의 `npm.cmd exec --yes --package=node@22 -- npm run check` 전체 PASS: 96 files / 990 tests, opt-in live 85 skip, typecheck/build PASS, lint 0 errors/기존 7 warnings.
- 수정 뒤 회상·라벨·계약·continuity·Dual/Responses 집중 5 files / 36 tests PASS(추가 oracle 회귀 이전 집계). 최종 집계는 아래 최종 확인에 기록한다.
- `python -m unittest test_retriever.py test_app.py`: 25 PASS.
- `python eval/run_product_eval.py`: top1 71/90, top5 87/90, negative 16/16, narrow 7/7, 생활어 2/2 PASS. 후속04 대비 같은 점수이며 이 단계에서 검색 threshold/corpus를 바꾸지 않았다.
- snapshot 생성 검사: 대화 7 tables, pending SQL 0. 기존 0014~0018 SQL은 main과 차이 없음.
- DB CI 상당 검사 최초 39 tests: 26 PASS/13 FAIL. WSL Bash 실행 불가 및 정확한 CPython 3.12.13 부재. Git Bash로 CLI 12개만 재시도: 7 PASS/5 FAIL(Windows 절대 경로/신뢰된 Python 경로). 이를 애플리케이션 결함이나 Linux CI PASS로 오인하지 않는다. Linux CI 전체 green은 남아 있다.

### 실제 provider / 고정 이력 / 로그인 연속 대화

1. 초기 익명 고정 이력 29개: 요청 29/29 도달, 계약 23 PASS/6 FAIL, CLI exit 1. 이때 로컬 RAG 내부 인증 설정 누락으로 retrieve 401. 검색 장애 fallback은 HTTP 요청 실패와 별도로 기록됐다. 가드를 끄지 않았다.
2. 정상 RAG 인증 후 **영향받은 12개만** 재실행: 요청 12/12, 계약 12 PASS. 원본 결과를 덮지 않았다. 이는 새 환경에서 전체 29개를 다시 통과시킨 결과가 아니다.
3. 최초 PG16 로그인 7턴: 저장·재로그인 복원 성공, 4·6·7 recall PASS였으나 당시 자동 oracle이 없던 3번의 10일 오답을 사람이 발견했다.
4. 정정 수정 후 새 계정의 실제 7턴: 모두 saved, 같은 conversation_id, 3·4·6·7 PASS; 1·2·5는 당시 ORACLE_UNCERTAIN. 6번 summary ready/extractive-v2/checkpoint10/included=true, 재로그인 7번 checkpoint10+recent2. 이후 2번 계약 보강을 소급해 새 live PASS로 부르지 않는다.
   - 보강 후 저장된 최종 7턴의 오프라인 재평가: 4 PASS(3·4·6·7), 1 FAIL(2), 2 ORACLE_UNCERTAIN(1·5). 추가 provider 호출 없음.
5. 1번/2번의 안전성·실용성·출처 검토는 위 결함과 같이 자동 PASS와 분리한다. OpenAI Responses/SKT 실제 provider는 미실행, 공통 전달 경계만 Mock 회귀.

raw 합성 응답/결과는 Git 제외 `product/.runtime/followup07/`에만 있다. 수정/재현에 사용한 사례를 독립 평가로 재분류하지 않았다. 과거 고정 이력과 실제 반환 conversation_id를 잇는 DB 상담은 별도다.

### 실제 PG16 / HTTP / worker

- 새로 생성하지 않고 기존 후속06 DB의 PG16.15, ledger20/aligned, 후조건을 재확인했다. 빈 DB 전체 적용·최소권한·10/20·stale lease·늦은 완료/cascade는 후속06의 실제 증거를 유지한다. 이번에 그 전체를 새로 수행했다고 하지 않는다.
- `run-followup07-http-acceptance.mjs`: 새 합성 계정에서 first request 완료 응답 동시 replay, 턴1개 유지 PASS.
- 해당 상담 ID에만 작동하는 일시적 PG trigger(08006)로 저장 실패 → 턴 증가 없음 → trigger 제거 → 같은 comparison_id로 저장 재시도 PASS. 일반 로그에 원문/비밀 노출 없음.
- 해당 상담 삭제 후 같은 요청 재전송404, thread0 PASS. 새 계정 삭제로 그 검사 데이터만 정리했다.
- worker 실제 entry point 2회 별도 프로세스 실행: 각각 deleted0/candidates0/summarized0/failed0, exit0. 이는 crash-reclaim 재현이 아니라 중복 실행/재시작 무작업 안정성이다.
- `--import-only`: 별도 합성 계정에서 동일 import_id의 동시2회+추가 retry, 동일 상담/1turn/reused=true, 위조 owner 무시 PASS. 이 HTTP 검사는 브라우저 동의 클릭과 별도다.

### 브라우저

실제 Chrome에서 신규 합성 가입·질문·저장·6턴·새로고침 목록·detail 복원·로그아웃·앱 재시작·재로그인·7번째 이어하기까지 관찰했다. 15일/다음 주 보존, 과거10일 원문 보존, 모델 카드와 출처 표시 유지.

- B(푸른건설) 페이지에서 A(한빛테크) 상담 복원 → 현재 이름 A, 과거 턴 A. 현재 화면 회사로 변경 → 현재 B, 과거 A 유지. B에서 급여일 회상 → A의15일을 혼입하지 않고 확인 필요. 연결 해제 → 일반 노동 상담, 과거 A/B 표기 유지.
- 실제 모델 번호 목록: 접근성 list marker와 화면에서 **1·2·3** 확인. 보이는 모델 카드 `Upstage Solar`는 정상 유지. 답변 본문의 provider 접두어/이전 답변 근거 라벨은 없었다. 공통 CSS/layout 변경 없음. 빈 줄·중첩·원문1반복 구조는 기존 SafeMarkdown 집중 회귀가 별도 담당한다.
- 위 두 테스트 상담은 UI에서 삭제했고 목록이 비었음을 확인했다. 다른 계정/후속06 기록은 삭제하지 않았다.
- 별도 익명 합성 질문(급여일25일)은 현재 탭 임시 보관 → 로그인 → 명시적 가져오기 동의 문구 → `동의하고 가져오기` → 정확히1개 상담/25일 복원. 실제 브라우저 동의와 HTTP import retry 검증을 혼동하지 않는다.
  - 가져오기 뒤 새로고침에서도 목록은 1개, 동의 재요청 없음. 해당 합성 상담도 UI 삭제 → 빈 목록 → 로그아웃을 확인하고 전용 탭을 닫았다.

### 호출·지연·토큰

비밀 없는 로컬 계측 합계: 실제 Upstage HTTP **89회, 모두200**, prompt146,768 / completion4,728 / total151,496 tokens. 고정 이력·두 연속 시도·브라우저 질문을 포함하며 한 사용자 질문이 한 모델 호출이라는 뜻이 아니다. 분류/재작성/생성 세부 배분은 계측하지 않았고 중계에 도달한 호출만 합산했다. 추출형 summary는 추가 모델0, 회상·저장-only retry도 추가 생성 없이 처리된다. 공급자 내부 retry/비용은 미측정이다.

- 공급자 개별 호출 지연: p50 569ms, p95 1,815ms, max2,179ms.
- 익명 eval41건의 요청 시작→최종응답 수신: p50 2,365ms, p95 3,333ms, max4,138ms. RAG 장애 초기29건+정상 재검12건이 섞인 관측 통계이며 성능 전후 향상률이 아니다.
- 브라우저 사용자 체감 총 지연(TTI/네트워크/렌더 포함)은 별도 미계측. 자동화 도구 왕복 시간을 서비스 지연으로 계산하지 않았다. 금액 추정은 하지 않았다.
- eval41건 답변 본문의 `Upstage Solar:`/`[이전 답변 근거:` 검출0. 출처/모델 카드의 정당한 표기를 삭제하는 검사는 아니다.

## 5. 공식 자료 대조 및 정책 잔여

기준일 2026-09-21에 공식 사이트를 직접 열어 확인했다. 근로기준법은 시행2026-08-20/법률21373, 임금채권보장법은 시행2026-08-20/법률21376, 시행령 제7조는 시행2025-10-23/대통령령35436 표시였다.

- [제56조](https://www.law.go.kr/법령/근로기준법/제56조): 22시~06시 실제 근로, 22시까지/이후 구분.
- [제11조](https://www.law.go.kr/법령/근로기준법/제11조), [시행령 제7조](https://www.law.go.kr/법령/근로기준법시행령/제7조), [별표1](https://www.law.go.kr/LSW/flDownload.do?bylClsCd=110201&flSeq=150840423&gubun=): 상시4명 이하 적용 규정에서 제56조 제외. 규모 미상은 확인, 별도 약정은 법정 가산과 구분.
- [고용노동부 노동포털](https://labor.moel.go.kr/minwonSysInfo/wagesolway.do): 온라인 진정/관할 방문과 조사 절차, 상담과 정식 접수 구분.
- [임금채권보장법 제12조](https://www.law.go.kr/법령/임금채권보장법/제12조), [정부24 확인서](https://www.gov.kr/mw/AA020InfoCappView.do?CappBizCD=14920000065): 대지급금·소송 목적, 감독 과정 확인, 발급 뒤 별도 지급요건. 단순 미지급만으로 자동 발급·지급 보장 아님.

승석/API: same-origin fetch metadata는 인증/인가가 아니다. 기존 owner/role/필드 제한 회귀는 유지. 공개 counts/열거·페이지/rate limit, risk/model 공개 범위, 익명 quota, feedback 연결·보존은 미확정. 운영 권한/대량 조회/침투 테스트는 하지 않았다.

나연/DB: 7 conversation tables와 favorites, 초안 event/consent 책임 경계 및 snapshot 복구 유지. 원문 중복 저장 목적·고지/동의/보호, 교차 기능 이벤트 필요성,30/90일·backup 삭제, 외부 전송 범위/공급자/TTL/철회, 탈퇴 범위, crash-pending 요청 처리 정책은 미확정. 운영0019·role·timer 설치는 미실행. 초안 두 테이블/MAS를 구현하지 않았다.

## 6. 재개 명령과 운영 전후 체크

로컬 실행(원본 .env/비밀을 출력하지 않는다):

```powershell
# 저장된 chroma-live 복사본이 있으면 준비를 반복하지 않는다.
cd db
npm.cmd exec --yes --package=node@22 -- node scripts/run-followup07-local.mjs
# READY 뒤 product/.runtime/followup07/ 에 evaluate, continuity, worker, restart, metrics 제어 파일 사용
# evaluate 내용이 AQ ID CSV이면 선택 실행. 한 번에 1회만 호출.
cd ../product
npm.cmd exec --yes --package=node@22 -- node scripts/run-followup07-http-acceptance.mjs
npm.cmd exec --yes --package=node@22 -- node scripts/run-followup07-http-acceptance.mjs --import-only
```

사용한 집중/전체 검사 명령(각 폴더에서 실행):

```powershell
# product/
npm.cmd exec --yes --package=node@22 -- npm run check
npm.cmd exec --yes --package=node@22 -- npm run build
npm.cmd exec --yes --package=node@22 -- node node_modules/vitest/vitest.mjs run src/services/conversationRecallService.test.ts src/services/conversationContinuityEvalRunner.test.ts src/services/conversationMemoryPropagation.test.ts src/components/chat/runLabels.test.ts src/services/answerContractCorpus.test.ts
node --check scripts/run-followup07-http-acceptance.mjs
# db/ (snapshot 검사에는 --repair를 붙이지 않음)
node --import tsx scripts/repair-conversation-snapshot.ts
node --check scripts/run-followup07-local.mjs
node --test tests/migration-drift.test.mjs tests/ingest-cli.test.mjs tests/ingest-industrial-safety-cli.test.mjs tests/path-b-bootstrap-static.test.mjs tests/path-b-canonical-timestamp-static.test.mjs tests/path-b-release-gate-static.test.mjs tests/path-b-sql-semantic-static.test.mjs
# product/integrations/rag-api/
.venv\Scripts\python.exe -m unittest test_retriever.py test_app.py
.venv\Scripts\python.exe eval/run_product_eval.py
```

DB CLI 재시도는 현재 프로세스 PATH의 맨 앞에 `C:\Program Files\Git\bin`을 추가해 Bash 선택만 바꿨다. Linux의 CPython 3.12.13 및 trusted Python/절대 경로 검사는 대체하지 못했다. 재개는 `.github/workflows/ci.yml`의 정확한 Python/Bash 환경에서 수행한다. 기존 사용자 DB를 CI 대상으로 바꾸지 않는다.

원본 봉인 자산은 유지했다. 실행기의 `chroma-live`가 없으면 `prepare_rag_assets.py stage-runtime --runtime-rag-db <이번 검사 전용 절대 경로>`로 준비하되 HF cache/원본 Chroma/manifest의 검증된 절대 경로를 명시한다. 준비되지 않은 환경에서 임의 다운로드·원본 교체로 우회하지 않는다. 실행 종료는 전용 제어 디렉터리의 `stop` 파일로 요청한다. 별도 세션 비밀 파일은 필요하지 않다.

운영 반영은 사용자가 정책/품질 차단을 해소한 뒤 수행한다. 이번 로컬 변경은 **배포 전까지 운영에 반영되지 않는다**.

1. 최종 diff/비밀 없음/새 원격main 재확인 → 사용자가 add/commit/push/PR. 이 보고서는 merge 승인 아님.
2. Linux DB/product CI, 제36조·실제 연속 답변 출처/실용성 재검증, 정책 결정 및 `CONVERSATION_MAINTENANCE.md` 검토.
3. 운영 대상·백업/복원 가능성·현재 SHA/ledger 확인. 승인된 migration→role→앱→worker 순서. drift에 따라 배포 가드 처리. 이번 작업에서는 실행하지 않음.
4. 반영 후 최소 smoke: live/ready·SHA·ledger·권한 확인, 합성 계정1개로 15일/다음 주 저장·복원·재로그인, A→B→해제/과거 턴, 익명 import1개, 공식 출처를 포함한 임금 질문·5인/22시·1350/진정 구분, 자기 합성 대화 삭제. worker journal과 만료/reclaim 집계에 원문/비밀 없는지 확인.
5. 문제 시 배포 중단/worker 중단·이전 앱 호환성 검토·백업 절차에 따른 복구. ledger/기적용SQL을 임의 수정하거나 DB를 초기화하지 않는다.

## 7. PR 초안

제목: `fix(chat): 통합 인수에서 발견한 정정 회상·표시 오류 보완`

본문:

- 현재 정정 진술을 옛 기억보다 우선하고, 사용자 진술 정리와 법령 검색 실패 라벨 분리
- 기존 격리 PG16 재사용 실행기와 실제 HTTP 중복·저장 재시도·import 검사 추가
- provider/연속 대화/브라우저/DB 증거 및 제36조 적용 실패를 개발 계약·인수 보고서에 기록
- 검증: product check, 집중 회귀, 실제 Upstage·PG16·Chrome(정확한 최종 범위는 본 보고서)
- 주의: 단계13 미완료. 제36조/출처 적합성, Linux DB CI, 팀 정책·운영 적용은 별도 차단. 자동 배포/전체 품질 승인 아님

## 8. 최종 확인

- 최종 product 테스트: **96 files / 995 PASS, live intent 85 SKIP**. 타입 검사 PASS, lint 0 errors/기존 7 warnings. 최초 최종 check의 빌드는 이번 작업 초기에 만든 임시 `chroma` 복사본의 Windows 접근 권한 때문에 중단됐다. 해당 전용 복사본만 제거한 뒤 빌드 단계만 재실행해 **production build PASS(exit0)**. 즉 전체 check의 구성 검사가 최종 통과했으며 최초 명령의 exit1 기록을 감추지 않는다. 원본 봉인 자산과 실제 검사에 사용한 `chroma-live`는 보존했다.
- 브라우저용 상담 3개를 UI에서 삭제했다. 연속 실행기가 만든 두 상담은 정확한 ID와 합성 소유자/7턴을 확인한 뒤 삭제했고 남은 대상 수0을 확인했다. HTTP 장애/import 검사의 자체 계정 정리도 PASS. 이 삭제는 복구 UI가 없으며 합성 실행 응답만 Git 제외 JSONL에 남아 있다. 다른 계정/후속06 상담은 삭제하지 않았다.
- 격리 컨테이너는 `exited`, 이번 앱/RAG/계측 포트는 listener 없음. volume은 보존했다. 무작위 비밀번호의 검사 역할과 합성 계정·회사 FK 행은 격리 DB 안에만 남아 있으며 운영 계정/역할과 무관하다.
- 최종 원격 main 재조회도 `4b9eae4`로 동일. Git 쓰기 없음. 로컬 수정은 미커밋·미배포다.

변경 파일:

- `product/src/services/conversationRecallService.ts`, 동명 test: 현재 정정 진술 우선 처리/회귀.
- `product/src/components/chat/ChatPanel.tsx`, `runLabels.ts`, `runLabels.test.ts`: 회상과 검색 실패의 표시 분리.
- `product/eval/answer-contract-cases.json`, `conversation-continuity-cases.json`, `product/src/services/answerContractCorpus.test.ts`: AQ30 개발 사례 및 정정/기록 계약.
- `db/scripts/run-followup07-local.mjs`, `product/scripts/run-followup07-http-acceptance.mjs`: 고정 격리 환경의 로컬 통합/HTTP 실행기.
- 본 보고서와 Git 제외 `AGENTS.md`: 증거·차단·인계.

다음 시작점은 **단계13 잔여 마감**이다. (1) 제36조 적용 조건/기산점 오류와 임금 질문의 관련 근거·구체적 기록 안내, AQ21 공개 안전 신호 의미를 먼저 수정·검증, (2) Linux DB CI, (3) 팀 정책 결정, (4) 사용자 승인 운영 반영과 최소 smoke 순서다. 새 provider 반복은 이 실패에 대한 수정 등 새 근거가 생긴 뒤 한정한다. 소스/Mock·실제 provider·실제 PG16·브라우저의 개별 통과는 서로 대체하지 않으며, 모든 선행 업무와 필수 인수가 끝나기 전 MAS 논의로 넘어가지 않는다.
