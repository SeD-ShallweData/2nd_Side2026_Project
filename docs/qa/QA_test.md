# QA 테스트 계획 (실제 데모 화면 기준, 최종 통합본)

\---

## 인수 테스트(Acceptance Test) 체크리스트 — 권한·게시판·위험카드

|번호|영역|항목|채점 기준 (O/X)|결과|
|-|-|-|-|-|
|1|사업장 검색|자동 선택 방지|"건설" 등으로 검색 시 결과가 다건 노출되고, 화면 안내("첫 번째 결과가 자동 선택되지 않습니다")대로 첫 번째 결과가 자동으로 선택되지 않는다|O|
|2|사업장 검색|동명 사업장 구분|검색 결과 각 카드에 지역·업종·"일부 일치" 등 구분 정보가 함께 표시되어 동명 사업장을 구분할 수 있다|O(조건부 구현)|
|2-1|사업장 검색|지역·업종 필터|검색 결과 화면에서 필터로 지역·업종을 선택하면 해당 조건에 맞는 사업장만 노출되고, 필터 해제 시 전체 결과로 복원된다|O|
|3|사업장 검색|읽기 전용 DB 고지|검색 화면에 "READ ONLY DB" 배지가 노출되고, 실제로 검색·조회 동작에서 데이터 변경이 발생하지 않는다|O|
|4|사업장 검색|페이지네이션|검색 결과가 10개 단위로 분할되어 노출되고, 페이지 이동이 정상 동작한다|O|
|5|위험카드|임금·산업안전 카드 분리|사업장 상세 화면에서 "임금 지급 관련 정보"와 "산업재해 확인 우선순위 신호"가 별도 카드로 분리되어 있고 합산 점수가 존재하지 않는다|O|
|6|위험카드|비낙인 고지 배너|카드 하단에 "결과를 하나의 점수로 합치지 않습니다" 문구와 함께, 두 신호 모두 사고확률·안전판정이 아니라는 고지 문구가 노출된다|O|
|7|위험카드|데이터 결측 표시|임금 카드의 '추가 공개 지표' 영역(이직률·고용 추이·데이터 충실도)이 빈 값(예: "확인할 수 없음")으로 표시되는지만 확인한다|O|
|8|위험카드|일치 결과 없음의 오독 방지|체불사업주 명단에 일치 결과가 없는 경우 "안전"으로 해석되지 않도록, "연계 데이터 범위에 존재하지 않는다는 의미이며 실제 신뢰 여부와는 관계가 없다"는 고지 문구가 함께 표시된다|O|
|9|위험카드|데이터 기준일·출처 표기|임금·산재 카드의 데이터 신뢰도·기준일 필드가 빈 값 없이(값이 없을 경우 정상적으로 빈 값으로) 표시되는지만 확인한다|O|
|9-1|위험카드(배지 종류별 정확성)|공식 명단 등재(우선 확인 필요)|배지가 '우선 확인 필요' 상태로 뜨는지 확인|O|
|9-1|위험카드(배지 종류별 정확성)|명단 일치 결과 없음(뚜렷한 이상 신호 없음)|"안전 인증"으로 안 읽히는지 확인|O|
|9-1|위험카드(배지 종류별 정확성)|임금 "안전 신호 미확인"|배지가 "안전 신호 미확인"으로 노출되는지 확인|O|
|9-1|위험카드(배지 종류별 정확성)|산재 "우선 확인 필요"|"검증된 사업장 사고 확률이 아닙니다." 문구 노출 확인|O|
|9-1|위험카드(배지 종류별 정확성)|산재 "뚜렷한 이상 신호 없음"|—|O|
|9-2|위험카드(데이터 신뢰도 등급 표시)|분석 자료 부족|"조회 실패"가 아니라 "자료 부족" 상태로 뜨는지|O|
|9-2|위험카드(데이터 신뢰도 등급 표시)|동명이인 사업장|시도·업종으로 구분되고 자동 확정 안 되는지|O(조건부 구현. 2번과 동일)|
|9-2|위험카드(데이터 신뢰도 등급 표시)|감독관 큐 대상|일반 사용자 화면에는 원점수가 아예 노출되지 않는지 확인한다 감독관 화면에만 "0\~1 상대 점수·발생확률 아님"이 표기되는지도 함께 확인|O|
|10|체크리스트|상태 초기화 정책|입사 전 행동 체크리스트(8개 항목)는 새로고침 시 의도대로 초기화되며(브라우저 로컬 상태만 유지), 이 동작이 데이터 유실 버그가 아님을 화면 안내 문구로 확인한다|O|
|11|체크리스트|카운터 정확성|항목 체크 시 상단 카운터("0/8 확인")가 실시간으로 정확히 갱신된다|O|
|12|화면 분리|상세 화면 범위 제한|사업장 상세 화면은 신호·체크리스트까지만 다루고, AI 상담·계약서 검토는 버튼을 눌러 별도 화면으로 이동해야만 진입된다|O|
|13|커뮤니티|기능 안내 고지|화면 상단에 "안내" 배지와 함께 "로그인 사용자는 게시글 작성과 신고를 이용할 수 있습니다. 댓글·공감 기능은 현재 제공하지 않으며 표시되는 수치는 조회용입니다." 안내가 노출된다|O|
|13-1|커뮤니티|카테고리 필터|"전체 / 입사 전 확인 / 근로계약서 / 현장 안전 / 임금" 선택 시 해당 카테고리의 게시글만 조회된다|O|
|13-2|커뮤니티|게시글 검색|검색창에 검색어 입력 시 검색 조건에 맞는 게시글 목록이 조회되며, 검색어 변경 시 결과가 갱신된다|O|
|13-3|커뮤니티|게시글 상세 조회|게시글 선택 시 해당 게시글의 상세 화면으로 이동하고 제목·본문·카테고리 등의 게시글 정보가 정상적으로 표시된다|O|
|13-4|커뮤니티|게시글 작성 권한|비로그인 상태에서는 글 작성 기능이 노출되지 않고, 로그인 상태에서는 게시글 작성 기능을 이용할 수 있다|O|
|13-5|커뮤니티|게시글 작성|로그인 사용자가 카테고리·제목·본문·익명 여부를 입력해 게시글을 작성할 수 있고, 작성 성공 후 해당 게시글 상세 화면으로 이동한다|O|
|13-6|커뮤니티|게시글 신고|신고 권한이 있는 게시글에서만 신고 기능이 노출된다. 본인 게시글에는 신고 기능이 노출되지 않으며, 신고 성공 시 완료 상태가 표시된다|O|
|13-7|커뮤니티|게시글 수정|본인 게시글 중 수정 권한이 있는 경우에만 수정 기능이 노출된다. 수정 화면에 기존 값이 표시되고, 변경 후 저장하면 수정된 내용이 상세 화면에 반영된다|보류|
|13-8|커뮤니티|게시글 삭제|본인이 작성한 게시글에서 삭제 기능이 노출되며, 삭제 확인 후 커뮤니티 목록으로 이동하고 삭제된 게시글은 더 이상 조회되지 않는다|O|
|13-9|커뮤니티|페이지네이션|게시글이 한 페이지 기준 개수를 초과하는 경우 페이지 이동이 가능하고, 페이지 이동 시 해당 페이지의 게시글이 조회된다|O|
|14 (미구현 대기)|권한|감독관 화면 접근 제한|근로감독관 대시보드는 별도 인증을 거친 계정만 진입 가능하고, 일반 사용자 계정으로는 접근이 차단된다. **민규 M2 완료 후 실행**||
|15|커뮤니티|게시글 데이터 표시|실제 API/DB 연동 환경에서 작성한 게시글이 목록 및 상세 화면에 정상적으로 반영되고, 이후 다시 조회했을 때도 작성 내용이 유지된다.|O|
|16|AI 상담|사업장 컨텍스트 연동|"이 사업장을 기준으로 AI 상담" 버튼으로 진입 시 해당 사업장 정보가 재입력 없이 답변에 반영되고, "지역과 업종을 보고 해당 사업장을 다시 선택해 주세요." 안내가 노출된다|O|
|17 (🔄 안 B 확정, 교체 예정)|AI 상담|두 모델 병렬 비교|같은 질문에 대해 Upstage Solar와 SKT A.X 답변이 동일 화면에 나란히 비교 표시되고, "동일 질문을 실제 API에 병렬 전송합니다" 고지가 노출된다. **코드 변경 전까지는 현재 듀얼 동작이 그대로 유효하므로 지금은 이 기준으로 테스트하고, 단일 전환 배포 후 아래 17-5로 교체한다**|O|
|17-1|AI 상담|판정 요구 방어|빠른 질문 버튼 "이 회사는 안전한가요?" 클릭 시, AI가 안전 여부를 단정하지 않고 확인 항목·다음 행동으로 응답한다|O|
|17-2|AI 상담|전문가 상담 대체 아님 고지|화면 상단에 "법률 판단이나 전문가 상담을 대신하지 않습니다" 문구가 노출된다|O|
|17-3|AI 상담|범위 밖 질문 처리|실제로 근로와 무관한 질문(예: "부동산 시세가 어떻게 되나요?")을 입력해, 억지 답변 없이 사유와 공식 창구만 안내되는지 확인한다. 이 경로는 LLM을 호출하지 않는 것으로 보여 반복 테스트해도 API 크레딧이 들지 않을 수 있다|X|
|17-4|AI 상담|API 키 비노출|브라우저 개발자도구 → Network 탭 → 상담 요청 선택 → Headers·Payload를 확인해 API 키 문자열이 없는지 확인한다. 서버가 대신 호출하는 구조라 키가 보이면 그 자체가 실패다|O|
|**17-5 (신규, 🕒 단일 전환 배포 후)**|AI 상담|단일 모델 전환·이중화 상실 리스크|(a) 단일 전환 후 응답이 한 모델에서만 오는지, 비교 UI가 한 칸으로 줄었는지 확인한다. (b) **단일 전환의 알려진 단점 검증**: 남은 provider(예: Upstage Solar)가 장애 상태일 때 기존 `catch → fallbackResult`로 다른 provider가 답변을 채워주던 것과 달리, **상담 전체가 정지되는지**를 의도적으로 확인한다(이건 회귀가 아니라 단일 전환을 선택하며 감수하기로 한 리스크이므로, 실제로 그렇게 동작하는 게 "정상"이다)||
|18|AI 상담|긴급 상황 차단|"산업재해로 다쳤다" 등 긴급 사고 표현 입력 시, 상담 답변 생성 전에 119·안전 확보 안내를 우선 반환한다|O|
|19|계약서 진단|결과 3분류|계약서 업로드 시 "확인된 항목 / 누락 가능 항목 / 회사에 다시 물어볼 질문" 3분류로 항목별 결과가 제공된다(결과 화면 배지 기준: "문서에서 확인됨 / 누락 가능 / 추가 확인")|O|
|19-1|계약서 진단|파일 형식·용량 제한|"PDF, PNG, JPG · 최대 10MB" 조건에 맞지 않는 파일 업로드 시 적절한 오류 메시지가 노출된다|O|
|19-2|계약서 진단|원문 미저장 고지|"파일은 내부 계약서 분석 서비스로 전달되며 원문을 제품 서버나 Git에 저장하지 않습니다" 문구가 노출되고, 실제로 원문이 영구 저장되지 않는다|O|
|19-3|계약서 진단|검토 버튼 활성화 조건|파일을 선택하기 전에는 "선택한 파일 검토" 버튼이 비활성 상태이며, 파일 선택 후에만 활성화된다|O|
|19-4|계약서 진단|비로그인 이용 가능 여부|로그아웃 상태(게스트)에서도 근로계약서 업로드→검토 결과 확인까지 로그인 유도 없이 전체 플로우가 정상 완료된다||
|20|권한/랜딩|firm\_id 유무 기준 랜딩 분기|로그인한 일반 사용자 계정의 firm\_id가 비어 있으면 사업장 검색 화면, firm\_id가 채워져 있으면 "내 사업장 게시글·리뷰" 화면으로 진입한다. 분기 기준은 firm\_id 유무 하나뿐이며 role 값은 사용하지 않는다(role 컬럼 자체가 삭제될 예정, auth\_role만 남음). 감독관은 2차 인증 후 대시보드, 관리자는 신고 관리 화면으로 진입한다||
|21|권한/데이터 스코프|firm\_id 스코프 제한 (users\_firm\_scope\_ck)|firm\_id가 있는 계정이 자신의 firm\_id 사업장 데이터만 조회 가능하고, 타 firm\_id 사업장의 "내 사업장" 화면·데이터에는 접근이 차단된다||
|22|권한|로그인 실패 잠금|5회 연속 로그인 실패 시 해당 계정이 15분간 잠기고, 15분 경과 후 재시도 가능하다. 이 상태는 서버 메모리에만 저장되므로 서버가 재시작되면 잠금이 초기화된다 — 이는 버그가 아니라 의도된 동작이며, 재시작 후 잠금 없이 로그인이 즉시 가능한 것이 정상이다||
|23|권한/커뮤니티|관리자 신고 처리(숨김)|관리자가 신고 관리 화면에서 신고 접수된 게시글을 확인하고 "숨김(hidden)" 처리하면, 해당 게시글이 커뮤니티 목록·검색·상세 조회에서 더 이상 노출되지 않는다. 🔶 신고 누적 임계치·경고/삭제/정지 등 추가 조치 단계, 기각된 신고의 재신고 가능 여부는 별도 초안·나연 회신 대기 중이라 이번 케이스는 "확인 → 숨김"까지만 검증한다||
|24 (신규, 🔴 인프라·담당자 결정 후)|현장 위험 제보|접수|현장 위험 상황을 사진과 함께 신고할 수 있고, 제출 후 접수 완료 상태가 표시된다. 🔴 사진 저장 위치(VM 디스크/GCS)·보존기간·개인정보(얼굴·차량번호) 마스킹 정책이 전부 미정이라 이 항목은 저장 인프라가 결정되기 전까지 테스트 불가||
|25 (🔴 인프라·담당자 결정 후)|현장 위험 제보|감독관 전용 열람 제한|현장 위험 제보 내역은 감독관 계정에서만 조회 가능하고, 일반 사용자·사업주 계정으로는 접근이 차단된다. 접근 권한(신고자 본인 열람 가능 여부 포함)도 아직 미확정||
|26 (🔴 인프라·담당자 결정 후)|현장 위험 제보|첨부 이미지 EXIF 제거|신고 시 첨부한 이미지의 위치정보 등 EXIF 메타데이터가 서버 저장 전 제거되는지 확인한다. ⚠️ 계약서 업로드(19-2)처럼 "원문 미저장"이 아니라 **사진은 신고 증거라 반드시 저장해야 하는 다른 요구사항**이므로 계약서 사례를 그대로 적용하면 안 된다||
|27 (🕒 화면 완료 후)|인증|로그인·회원가입·로그아웃|회원가입(이메일·비밀번호 등) → 로그인 → 로그아웃까지 전체 플로우가 오류 없이 동작하고, 로그아웃 후 인증이 필요한 화면 접근 시 로그인 화면으로 안내된다||
|28 (🕒 화면 완료 후)|인증|firm\_id "검증 없음" 문구|firm\_id 연결 화면에 "검증 없음" 문구가 노출되고, 실제로 사업자등록증 등 확인 절차 없이 입력만으로 firm\_id가 연결된다||

\---

## 테스트에 필요한 계정·게시글·사업장 상태 정의

### 계정

|계정 유형|상태|용도|
|-|-|-|
|미로그인(게스트)|-|사업장 검색·위험카드·AI상담·계약서 검토 비로그인 플로우 확인, 커뮤니티는 열람·필터·검색만 가능한지 확인|
|일반 회원 A (firm\_id 없음)|정상 활성|검색·위험카드 조회·체크리스트·AI 상담·계약서 업로드 전체 플로우 테스트|
|일반 회원 B (firm\_id 없음)|정상 활성|커뮤니티 게시글 작성·수정·삭제·타인 글 신고 테스트용(A와 상호작용 확인)|
|**일반 회원 C (firm\_id 보유, 신규)**|정상 활성|로그인 시 "내 사업장 게시글·리뷰" 화면으로 랜딩되는지, 자기 사업장 신호 카드를 열람할 수 있는지 확인 (20번 검증용)|
|**일반 회원 D (다른 firm\_id 보유, 신규)**|정상 활성|C가 D의 사업장 데이터에 접근하지 못하는지 firm\_id 스코프 교차 확인 (21번 검증용)|
|근로감독관 계정|별도 인증 완료|감독관 대시보드 접근 및 일반 계정의 접근 차단 확인|
|관리자/운영자|관리자 권한|신고된 게시글 처리, 위험카드 데이터 표시 오류 등 백오피스 확인|
|정지된 계정|이용 제한|제한된 계정이 실제로 글 작성·상담 이용이 차단되는지 확인|
|**잠금 테스트 계정 (신규)**|정상 활성|5회 연속 로그인 실패 유도 → 15분 잠금 및 해제 확인 (22번 검증용)|

### 게시글

> ⚠️ 커뮤니티 글쓰기·신고 기능은 이미 로그인 사용자 기준으로 실동작하는 것으로 업데이트됨(13번 항목 참고). 댓글·공감 기능만 아직 미제공. 아래 상태 중 "연동 후"로 표시된 항목은 신고 임계치·삭제 처리 등 후속 로직이 실제 배포된 뒤 테스트한다.

|상태|설명|확인 목적|
|-|-|-|
|정상 게시글(더미 데이터)|화면 구조 확인용 더미 게시글(태그·지역·업종·작성자유형·시간·공감·댓글 수 포함)|목록·상세 조회 정상 노출, 카테고리 태그·메타정보 정확히 표시되는지|
|실제 작성 게시글|로그인 사용자가 13-5로 작성한 게시글|목록·상세에 정상 반영, 재조회 시 유지되는지 (15번 참고)|
|(연동 후) 신고 접수된 게시글|다른 사용자가 신고한 상태|신고 임계치 도달 시 노출 제한/관리자 검토 큐 이동 여부|
|(연동 후) 삭제된 게시글|작성자 또는 관리자가 삭제|삭제 후 목록·검색에서 사라지는지, 댓글은 어떻게 처리되는지|
|(연동 후) 익명 게시글|작성자 정보 비노출|익명성 보장되는지, 관리자 화면에서도 개인정보 과다 노출 없는지|

### 계약서 파일 (계약서 진단 테스트용)

|상태|설명|확인 목적|
|-|-|-|
|정상 근로계약서(PDF)|지원 형식·용량 이내 파일|확인된 항목/누락 가능/다시 물어볼 질문 3분류 정상 출력|
|정상 근로계약서(PNG/JPG)|이미지 형식 계약서|이미지 인식(OCR) 기반 구조화 추출이 PDF와 동일하게 동작하는지|
|용량 초과 파일|10MB 초과 파일|"최대 10MB" 제한 안내와 함께 업로드가 거부되는지|
|미지원 형식 파일|PDF/PNG/JPG 외 형식(예: docx, hwp)|적절한 오류 메시지가 노출되고 서버 오류로 이어지지 않는지|
|필수 항목 누락 계약서|임금 지급일 등 특정 항목이 비어있는 계약서|해당 항목이 "누락 가능"으로 정확히 분류되는지|

### 사업장 (배지·데이터 신뢰도 상태 기준)

|상태|설명|확인 목적|
|-|-|-|
|공식 명단 등재 사업장|체불사업주 명단에 실제 등재된 사업장|"공식 명단 1개 확인" 문구가 정확히 표시되고 사전 신호와 구분되는지|
|명단 일치 결과 없음 사업장|체불사업주 명단에 등재되지 않은 사업장|"연계 데이터 내 일치 결과 없음" 표시가 뜨고, 이것이 "안전"으로 오독되지 않도록 고지 문구가 함께 노출되는지|
|임금카드 "추가 확인 권장" 배지|임금 지표가 판단하기에 불충분하거나 애매한 사업장|배지와 함께 "현재 자료만으로 판단하기 어려워 근로조건을 추가로 확인하는 것이 좋습니다" 문구가 노출되는지|
|산재카드 "우선 확인 필요" 배지|공표된 산업안전 자료에서 우선 확인 범위가 높게 표시된 사업장|"이 신호는 해당 회사의 사고확률이 아닙니다" 고지 문구가 함께 노출되는지|
|산재카드 "뚜렷한 이상 신호 없음" 배지|우선 확인 범위가 낮거나 "일반"으로 표시된 사업장|"안전 인증"으로 오독되지 않고, 현장 확인 권장 문구가 함께 있는지|
|"분석 자료 부족"/"제한적 자료" 상태|데이터 신뢰도가 "제한적 자료"로 표시되는 사업장|카드가 정상 렌더링되고 낮은 신뢰도가 명확히 전달되는지|
|데이터 미연동 사업장|이직률·고용추이·데이터충실도 등 세부 지표 미연동|지표가 "확인할 수 없음"으로 표시되고 임의값·추정치로 채워지지 않는지|
|동명이인 사업장(2개 이상)|같은 회사명, 다른 지역/업종, "일부 일치" 배지|검색 시 지역·업종 구분 후보 노출 여부, 필터 적용 시 정확히 걸러지는지|
|신호 높은 사업장(감독관 큐 대상)|임금·산재 신호 수치가 높아 감독관 위험큐에 포함된 사업장|일반 사용자 화면에는 신호형 문구로만 노출되는지, 감독관 화면에서만 원점수가 "확률 아님"으로 표기되는지|
|**firm\_id 스코프 테스트용 사업장 2곳 (신규)**|각각 다른 firm\_id를 가진 사업장 A, B|일반 회원 C가 사업장 B의 "내 사업장" 화면에 접근 시도할 때 차단되는지 (21번 항목 검증용)|

\---

