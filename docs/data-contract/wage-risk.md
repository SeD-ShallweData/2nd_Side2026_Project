# 임금체불 데이터 계약

- 계약 버전: `wage-risk.v1.0`
- 기준일: 2026-08-29
- 대상: PostgreSQL 16 / `wageguard` DB / `public` schema
- 기본 소비자: 구직자 웹 UI, 근로감독관 대시보드, LLM 상담 백엔드
- 검증 기준 배치: `batches.id = 7` (`as_of_date=2026-06-01`, `target_month=2026-12-01`)

## 1. 목적과 비목적

이 문서의 목적은 **DB에 저장된 값이 무엇을 뜻하는지** 고정하는 것이다.
화면 문구·색상·카드 배치 같은 표현 규칙은 `product/docs/service-policy.md`(POL-01~POL-14)가
이미 정의했으므로 여기서 다시 쓰지 않고 POL 번호로 참조한다.

API 응답 필드의 형태는 `product/docs/api-contract.md` 4절이 정의한다. 여기서는
**DB 원본 값 → 그 응답 필드** 사이의 변환 규칙만 다룬다.

이 문서는 모델을 설명하지 않는다. 모델 성능·학습 방법은 본선 제안서 3.2.3을 본다.

### 1.1 ML→DB 계약 문서와의 경계

계약 문서가 둘이다. **범위가 겹치지 않는다.**

```
docs/mlops/ml-db-data-contract.md      CSV 규격 · 컬럼 순서 · 적재 검증
        ↓  경계
        safe_recommendation.판정 6종 · scored_active 54컬럼 · inspector_queue 10컬럼
        ↓
docs/data-contract/**  (이 문서)       판정→표시 매핑 · 노출 금지 · 기준일 · 집계 정의
```

**CSV를 만드는 쪽은 앞 문서를, 화면·상담을 만드는 쪽은 이 문서를 본다.**

### 🟡 같은 필드를 서로 다른 이름으로 부른다

```
CSV (ML→DB 계약)     G1_고용안정 … G6_낮은변동성      ← 대문자 G
DB (이 문서)         g1_고용안정 … g6_낮은변동성      ← 소문자 g
```

**버그가 아니다.** `db/scripts/ingest.sh` 의 staging 테이블이 CSV의 대문자를 그대로 받고
(`"G1_고용안정" text`), 변환 단계에서 소문자 최종 컬럼에 넣는다.
`HEADER MATCH` 도 staging과 비교하므로 정합한다.

**두 문서를 오갈 때 이 변환을 기억한다.** 그 밖의 컬럼은 이름이 같다.

## 2. ⚠️ 사용자와 감독관은 서로 다른 테이블을 본다

**이 문서에서 가장 중요한 항목이다.**

```
구직자 화면    public.safe_recommendation.판정   (v_current_safe)
                  안정신호 / 유보 / 유보_정보부족 / 배제_○○

감독관 화면    public.scored_active.risk_tier    (v_current_scored)
                  매우높음 / 높음 / 다소높음 / 일반 / 정보부족 / 이미공개
                  + risk_full + SHAP 사유 + G1~G6
```

같은 사업장에 두 값이 모두 붙어 있다. **섞어 쓰면 안 된다.**

DB 주석이 이미 이를 명시하고 있다.

> `scored_active.risk_tier` — 감독관 전용, **구직자 화면에 노출 금지(명예훼손 리스크)**
> `v_current_safe` — **구직자 화면은 위험등급 대신 이 판정만 쓴다**
> `inspector_queue.queue_priority` — `scored_active.risk_tier` 와 **다른 척도다. 같은 단어로 부르지 말 것**

세 척도(`판정`·`risk_tier`·`queue_priority`)가 각각 다른 모집단·다른 기준으로 계산된다.
용어를 공유하지 않는다.

### 2.1 두 화면의 어휘 대응표 **[실측 2026-09-09 · 최신 배치 553,598]**

같은 사업장을 두 화면이 뭐라고 부르는지 전수 대조했다.
**「같은 뜻인데 말만 다른 것」과 「실제로 다르게 부르는 것」을 구분한다.**

| 구직자 판정 | 곳 | 감독관 등급 분포 | 대응 |
| --- | ---: | --- | :-: |
| `안정신호` | 32,613 | **일반 100%** | ✅ 완전 일치 |
| `배제_임금체불공개` | 133 | **이미공개 100%** | ✅ 완전 일치 |
| (판정 없음) | 49,711 | 정보부족 49,703 · 이미공개 8 | ✅ 사실상 일치 |
| `유보` | 431,646 | 일반 409,164 (94.8%) · 다소높음 22,397 · 높음 85 · **매우높음 0** | 🟡 넓게 퍼짐 |
| `배제_공개체납` | 1,102 | 다소높음 499 · 일반 315 · 높음 204 · 매우높음 84 | 🟡 |
| `배제_4대보험체납(door1)` | 20,863 | 다소높음 10,580 · 높음 4,595 · 일반 3,949 · 매우높음 1,739 | 🟡 |
| **`유보_정보부족`** | **17,530** | 일반 7,336 · 다소높음 6,825 · **높음 2,673** · **매우높음 696** | 🔴 **어긋남** |

역방향으로 보면 문제가 뚜렷하다.

| 감독관 등급 | 곳 | 구직자에게는 | |
| --- | ---: | --- | :-: |
| `매우높음` | 2,519 | `review` 1,823 (72.4%) · **`unknown` 696 (27.6%)** | 🔴 |
| `높음` | 7,557 | `review` 4,799 · **`unknown` 2,673 (35.4%)** · `watch` 85 | 🔴 |
| `다소높음` | 40,301 | `watch` 22,397 · `review` 11,079 · `unknown` 6,825 | 🟡 |
| `일반` | 453,377 | `watch` 409,164 · `normal` 32,613 · `unknown` 7,336 · `review` 4,264 | — |
| `정보부족` | 49,703 | `unknown` 100% | ✅ |
| `이미공개` | 141 | `review` 133 · `unknown` 8 | ✅ |

#### 🔴 읽는 법 — 어긋남은 `유보_정보부족` 한 곳에서 나온다

```
감독관 「매우높음」 2,519곳 중  696곳(27.6%)이  구직자에게 「분석 자료 부족」
감독관 「높음」    7,557곳 중 2,673곳(35.4%)이  구직자에게 「분석 자료 부족」
```

**원인은 규칙이 겹치기 때문이다** (3.1.1절).

```
n_months ≥ 6   → 채점됨 (risk_full 있음 → risk_tier 매겨짐)
n_months < 8   → 판정은 유보_정보부족
```

**6~7개월 구간**이 겹친다. 점수는 나오지만 안정신호 판정에는 관측이 부족하다고 본 곳이다.

- 구직자 쪽: *"평가할 만큼 자료가 충분하지 않다"* → 안전도 위험도 말하지 않는다
- 감독관 쪽: *"짧은 관측이지만 점수가 높으니 우선 보라"* → 점검 우선순위

**각각은 자기 목적에 맞다. 결함이 아니다.**
다만 **같은 회사를 다르게 부른다는 사실을 양쪽 화면이 모르고 있다.**

#### 🟢 어긋나지 않는 것 — `유보` 에는 `매우높음` 이 0곳이다

`유보` 431,646곳 중 감독관 `매우높음` 은 **0곳**, `높음` 은 85곳(0.02%)뿐이다.
**`유보` 가 「위험이 발견된 집단」이 아니라는 증거**이며,
2026-09-08 결정 1번(문구 「안전 신호 미확인」·회색)의 근거와 같다.

#### 미확정 — 화면 문구

감독관 점검 큐 3,000곳 중 **829곳(27.6%)** 이 구직자 화면에서 「분석 자료 부족」으로 보이고,
그중 **22곳은 큐에서 「긴급」** 이다(긴급 100곳 중 22%).

`risk_tier` 노출은 금지이므로 등급을 보여주자는 뜻이 아니다.
**「자료가 없다」와 「자료가 짧아 판단을 보류했다」를 구분**하는 문구가 필요하다 — 11.6절.

## 3. 사용자 경로 — `safe_recommendation.판정`

### 3.1 판정 값과 변환

변환 코드: `product/src/adapters/real/MlRiskProvider.ts` 의 `VERDICT_META`(56~75행)와
`toWageRiskPublic()`(76~125행).

| `판정` | `SignalLevel` | `Confidence` | 근거 코드 |
| --- | --- | --- | --- |
| `안정신호` | `normal` | `sufficient` | `SAFE_RECOMMENDATION_STABLE` |
| `유보` | `watch` | `sufficient` | `SAFE_RECOMMENDATION_HOLD` |
| `유보_정보부족` | `unknown` | `unavailable` | `INSUFFICIENT_HISTORY` |
| `배제_임금체불공개` | `review` | `sufficient` | `OFFICIAL_WAGE_LISTING_MATCH` |
| `배제_공개체납` | `review` | `sufficient` | `INSURANCE_PAYMENT_REVIEW` |
| `배제_4대보험체납(door1)` | `review` | `sufficient` | `INSURANCE_PAYMENT_REVIEW` |

**변환 우선순위**: `배제_` 접두 판정이 `VERDICT_META` 조회보다 **먼저** 적용된다.
코드가 `verdict?.startsWith("배제_")` 로 판단하므로, 새 `배제_*` 값이 추가되면
자동으로 `review` + `INSURANCE_PAYMENT_REVIEW` 가 된다.

`SignalLevel` 4종의 사용자 표기는 POL 상태 표현 정책 표를 따른다(normal=뚜렷한 이상 신호 없음,
watch=추가 확인 권장, review=우선 확인 필요, unknown=분석 자료 부족).

> 🔴 **`watch` 의 표기 「추가 확인 권장」은 재검토 대상이다.** 3.1.2절 참조.

### 3.1.1 판정이 정해지는 규칙

모델 담당 회신(2026-09-06) 기준. 산출 코드 `deploy_products_full.py` 의 `vd()` 와
`score_active_full.py` 의 `green()` 에서 확인된 값이며 **추측이 아니다.**

**위에서부터 먼저 걸리는 것으로 확정된다.**

```
1. 체불배제 = 1                                    → 배제_임금체불공개
2. 체납배제 = 1                                    → 배제_공개체납
3. door1_ever > 0                                  → 배제_4대보험체납(door1)
4. n_green ≥ 5  AND  risk_full ≤ p10  AND  n_months ≥ 8   → 안정신호
5. n_months < 8                                    → 유보_정보부족
6. 나머지 전부                                      → 유보
```

#### `안정신호` 는 3조건 AND 교집합이다

| 조건 | 내용 |
| --- | --- |
| `n_green ≥ 5` | 그린플래그 6개 중 **5개 이상** |
| `risk_full ≤ p10` | **평가군** 하위 10%. ⚠️ **배치마다 동적** (평가군 정의는 아래) |
| `n_months ≥ 8` | 관측 **8개월 이상** |

셋을 동시에 만족해야 하므로 통과율이 낮다 — 최신 배치 **6.5%**.

**`p10` 은 고정값이 아니다.** batch 7(`as_of=2026-06-01`) 실측 경계는 **`0.08237`** 이다
**[실측 2026-09-06 · 2026-09-09 재확인]**. 배치가 갱신되면 달라지므로 **문서에 상수로 박지 않는다.**

#### 🔴 「평가군」은 전체 채점군이 아니다 **[실측 2026-09-09]**

어느 모집단에서 재느냐에 따라 `p10` 이 달라진다. 네 가지를 모두 재 보았다.

| 모집단 | 곳 | `p10` | |
| --- | ---: | ---: | --- |
| 전체 채점군 | 503,887 | 0.085733 | |
| 배제 3종 제외 | 481,789 | 0.083720 | |
| **배제 제외 + `n_months ≥ 8`** | **464,259** | **0.082370** | ✅ |
| `안정신호` 의 실제 `risk_full` 상한 | 32,613 | **0.082370** | ✅ **일치** |

**평가군 = 배제 3종을 뺀 뒤 `n_months ≥ 8` 인 464,259곳**이다.
`안정신호` 집단의 실제 상한과 소수점 6자리까지 일치한다.

전체 채점군으로 재면 `0.0857` 이 나와 **약 4% 넓은 기준**이 된다.
평가군을 잘못 잡으면 안정신호 대상이 달라지므로 반드시 위 정의를 쓴다.

#### 그린플래그 6종

| | 이름 | 조건 |
| --- | --- | --- |
| G1 | 고용안정 | 인원 −5% 이내 |
| G2 | 성실납부 | 12개월 공백 없음 |
| G3 | 인건비안정 | 급여 −5% 이내 |
| G4 | 인력유지 | 신규 ≥ 상실 |
| G5 | 업력3년 | 적용일 기준 (코드상 `'2023-04'` 하드코딩 = **근사**) |
| G6 | 낮은변동성 | 인원 변동계수 ≤ 0.15 |

### 3.1.2 🔴 `유보` 는 「위험 발견」이 아니라 「안전 미확인」이다

**규칙 6번을 보면 `유보` 는 독립된 판정이 아니라 「나머지 전부」다.**

```
유보 = 배제 아님 + 정보부족 아님(n_months ≥ 8) + 안정신호 3조건 중 하나 이상 미달
```

즉 **판단해서 「주의」로 결론 낸 것이 아니라, 안정 신호를 확인하지 못한 것**이다.
85.7%가 여기 해당하는 것은 **안정신호 문턱을 높게 잡은 보수적 설계의 결과**이며 정상이다.

#### 실측 검증 **[2026-09-06]**

| 검사 | 결과 |
| --- | ---: |
| `안정신호` 중 3조건 위반 | **0건** |
| `유보` 중 `n_months < 8` | **0건** |
| `유보_정보부족` 중 `n_months ≥ 8` | **0건** |
| `유보` 중 3조건 전부 충족 (`p10 = 0.08237` 기준) | **0건** |

각 판정의 실제 값 범위

| 판정 | `n_green` | `n_months` | `risk_full` |
| --- | --- | --- | --- |
| `안정신호` | **5~6** | **8~12** | 0.0059~**0.08237** |
| `유보` | 0~6 | **8~12** | 0.0062~0.9337 |
| `유보_정보부족` | 0~5 | **5~7** | 0.0143~0.9784 |

**규칙과 데이터가 정확히 일치한다.**

#### 표기 재검토가 필요하다

현재 `watch` 의 화면 배지는 **「추가 확인 권장」**(`riskPresentation.ts`)이다.
이 문구는 **「확인할 만한 무언가를 발견했다」로 읽힌다.** 실제 뜻은 반대다.

모델 담당 권고 **[문서: 2026-09-06 회신]**

> ❌ 유보에 **"추가 확인 권장"·"주의"·"발견" 류 문구 금지**(위험 발견처럼 읽힘).
> 유보 = **회색(모름)**
>
> 프레임: **"안정신호만 초록, 위험 이력은 빨강, 나머지는 회색(모름)"**. 초록이 적은 게 정상.

| 판정 | 현재 | 권장 |
| --- | --- | --- |
| `안정신호` | 뚜렷한 이상 신호 없음 | **안정 신호 확인** 🟢 |
| `유보` | 추가 확인 권장 | **안전 신호 미확인 / 판단 보류** ⚪ |
| `유보_정보부족` | 분석 자료 부족 | 정보 부족 · 평가 보류 ⚪ |
| `배제_임금체불공개` | (미정) | 임금체불 공개 이력 🔴 |
| `배제_공개체납` | (미정) | 체납 공개 이력 🔴 |
| `배제_4대보험체납` | (미정) | 4대보험 체납 이력 🟠 |

> ⚠️ **`유보` 와 `유보_정보부족` 을 둘 다 회색으로 두면 구분이 어려워진다.**
> 색 배정은 화면 설계 영역이므로 **정보설계 담당 결정 사항**이다.
> 이 문서는 **「추가 확인 권장」이 부정확하다는 사실**까지만 규정한다.

**영향 범위** — 표기를 바꾸려면 아래가 함께 움직인다.

| 대상 | 내용 |
| --- | --- |
| `product/src/domain/riskPresentation.ts` | `SIGNAL_STATUS_META` 정본 |
| `docs/qa/qa_test_cases.md` **#9-1** | 기대 문구가 「추가 확인 권장」 |
| `docs/ui-spec/dashboard_spec.md` | 5종 배지 설계 |
| 이 문서 `README.md` 배지 매핑표 | |

### 3.2 실제 분포 (2026-08-29 실측, 최신 배치)

| `판정` | 사업장 수 | 비율 |
| --- | ---: | ---: |
| `유보` | 431,646 | **85.7%** |
| `안정신호` | 32,613 | 6.5% |
| `배제_4대보험체납(door1)` | 20,863 | 4.1% |
| `유보_정보부족` | 17,530 | 3.5% |
| `배제_공개체납` | 1,102 | 0.2% |
| `배제_임금체불공개` | 133 | 0.03% |
| **합계** | **503,887** | `batches.n_safe` 와 일치 |

**⚠️ 화면 설계에 영향을 주는 사실**: 조회 대상의 **85.7%가 `유보`(watch)** 다.
대부분이 같은 상태로 나오므로 `watch` 는 변별력이 낮다. `watch` 를 경고성 시각 요소로
강조하면 거의 모든 사업장이 경고로 보인다. 화면·정보설계 담당과 협의가 필요하다.

`배제_임금체불공개` 는 133곳(0.03%)뿐이다. 공식 명단 연계는 매우 드문 경우다.

### 3.3 사용자에게 노출하지 않는 것

POL-03 금지 목록을 따른다. 추가로 이 계약에서 명시한다.

- `scored_active` 의 모든 컬럼 (`risk_full`, `risk_tier`, `n_green`, `door1_*`, `nf_*` 등)
- `inspector_queue` 의 모든 컬럼
- `배제_` 판정의 원문 문자열 (`배제_4대보험체납(door1)` 등)

> ⚠️ **`g1~g6` 은 2026-09-08 결정으로 일부 공개된다.** 3.5절이 정본이다.
> **`n_green`(개수)은 계속 비공개**다.

`판정` 원문은 근거 코드(`INSURANCE_PAYMENT_REVIEW` 등)로 변환해 내보낸다.

### 3.4 LLM 상담에 넘어가는 범위 — `company_id` 가 있을 때만

**POL-07** — *"신호 조회, 회사 컨텍스트 상담, 이력 연결은 `company_id`를 사용한다.
사업장명이 비슷하다는 이유로 다른 회사의 수치나 공식 명단 상태를 인용하지 않는다."*

코드가 이 정책을 4단계로 강제한다 **[실측 2026-09-02]**.

| 단계 | 위치 | 동작 |
| ---: | --- | --- |
| 1 | `chatComparisonService.ts:155` | `if (request.company_id)` — 없으면 회사 조회 자체를 안 한다 |
| 2 | `chatComparisonService.ts:158` | `getCompanyRisk()` → `CompanyRiskResult`. **금지 필드가 타입에 없다** |
| 3 | `DualLlmChatProvider.ts:69-79` | `companyContext ? {…} : null` — 없으면 `company: null` 로 프롬프트 구성 |
| 4 | `DualLlmChatProvider.ts:124-125` | `context_mode` · `company_context_attached` 로 첨부 여부를 기록 |

프롬프트에 들어가는 회사 필드는 **7개뿐**이다.

```
company_id · company_name · address · region · industry · size_label
public_signal_result   ← CompanyRiskResult (구직자 응답과 같은 형태)
```

`public_signal_result` 는 **구직자 API 응답과 같은 타입**이라
`risk_full`·`risk_tier`·`shap_value`·`percentile`·`model_score` 가 **구조적으로 들어갈 수 없다**.

#### 대화 기억 구조에 적용할 것

| # | 규칙 | 이유 |
| ---: | --- | --- |
| 1 | 회사 컨텍스트는 **`company_id` 로만** 붙인다 | 사업장명 유추 금지 (POL-07) |
| 2 | 기억에 **ML 수치를 저장하지 않는다** | POL-03 금지 목록 |
| 3 | 저장은 **`company_id` + 조회 시점**까지. 값은 **매번 다시 조회**한다 | 배치가 갱신되면 값이 바뀐다. 기억된 값은 낡는다 |
| 4 | 감독관 컨텍스트를 외부 LLM에 보내려면 **명시적 승인**이 필요하다 | `inspectorPolicy.test.ts:8` 이 검사한다 |

> 3번이 특히 중요하다. `as_of_date` 는 6개월 주기로 바뀐다.
> **어제 기억한 `유보` 가 오늘도 `유보` 라는 보장이 없다.**

### 3.5 긍정 지표 G1~G6 의 공개 범위 **[2026-09-08 결정 7번]**

이 절이 정본이다. 3.3절의 전면 비공개 규칙보다 우선한다.

| 공개 | 비공개 |
| --- | --- |
| ✅ **6개 항목의 이름** | ❌ 원값 (`g1_고용안정` 등 boolean) |
| ✅ **확인된 항목의 체크 표시** | ❌ **개수 (`n_green`)** |
| | ❌ 점수·가중치 |
| | ❌ 판정 규칙에서의 역할(`n_green ≥ 5`) |

#### 공개할 6개 이름

감독관 화면(`inspectorService.ts:373-380`)과 **같은 라벨을 쓴다.**

| | 이름 | 조건 (내부용 · 화면에 쓰지 않는다) |
| --- | --- | --- |
| G1 | 고용 안정 | 인원 −5% 이내 |
| G2 | 성실 납부 | 12개월 공백 없음 |
| G3 | 인건비 안정 | 급여 −5% 이내 |
| G4 | 인력 유지 | 신규 ≥ 상실 |
| G5 | 업력 3년 | ⚠️ **이름과 실제가 다르다 — 11.7절** |
| G6 | 낮은 변동성 | 인원 변동계수 ≤ 0.15 |

> 🔴 **G5 는 화면 표기에 주의한다.** 코드 기준일이 고정돼 있어 현재 실제 경계는
> **38개월(3년 2개월)** 이며 배치마다 벌어진다. 「업력 약 3년 이상」으로 쓴다 (11.7절).

#### 🔴 현재 구현이 결정과 어긋난다 — `n_green` 개수가 이미 나가고 있다

`MlRiskProvider.ts:97-100` 이 **사용자용 위험카드 설명문**에 개수를 넣는다.

```ts
row.n_green === null
  ? "구직자 공개 판정에서 확인된 결과입니다."
  : `공개 판정에서 안정 신호 ${row.n_green}개가 확인됐으며, 다른 판정 조건과 함께 해석해야 합니다.`
```

이 분기는 `level !== "unknown"` 일 때 실행되므로 **`유보`(watch)와 `안정신호`(normal) 모두**에 적용된다.

**영향 규모** [실측 2026-09-09]

| 판정 | 곳 | `n_green ≥ 5` | 비중 |
| --- | ---: | ---: | ---: |
| `유보` | 431,646 | **256,596** | 59.4% |
| `안정신호` | 32,613 | 32,613 | 100% |
| 합계 | **464,259** | 289,209 | |

**`유보` 중 `n_green = 6`(만점)인 곳이 172,424곳**이다.

그 카드는 이렇게 보인다.

```
배지     안전 신호 미확인   (회색)
설명     공개 판정에서 안정 신호 6개가 확인됐으며 …
```

**같은 카드 안에서 정면으로 어긋난다.** 배지는 「확인 안 됨」인데 설명은 「6개 확인됨」이다.
2026-09-08 결정이 「개수 비공개」로 정한 이유가 여기에 있다.

#### 요청 항목

| # | 무엇 | 담당 |
| ---: | --- | --- |
| 1 | `MlRiskProvider.ts:97-100` 에서 **`n_green` 개수 제거** | 화면·어댑터 담당 |
| 2 | 위험카드에 **G1~G6 이름 + 체크 표시** 추가 | 화면 담당 |
| 3 | 상담 프롬프트에 같은 규칙 반영 (이름·체크만, 개수 금지) | 프롬프트 담당 |

**대안 문구** — 개수를 빼고 항목으로 바꾼다.

```
현재   공개 판정에서 안정 신호 6개가 확인됐으며, 다른 판정 조건과 함께 해석해야 합니다.
대안   아래 확인 항목 중 일부가 충족됐습니다. 충족 여부는 항목별 표시를 참고하세요.
```

#### 감독관 경로는 이 제한을 받지 않는다

`inspectorService.ts` 의 `green_count`·`green_flags` 는 **감독관 전용**이라 그대로 둔다.
2절의 경로 분리 원칙에 따른다.

## 4. 감독관 경로 — `scored_active.risk_tier`

### 4.1 등급 정의

`risk_tier_meta` 테이블이 정본이다. DB 주석:

> 위험등급 범례·툴팁용 해설표. **lift 는 ML팀 실측값이며 DB 에서 재계산 불가 — 임의로 고치지 말 것.**

| tier | label | percentile | recall_cum | lift | `is_prediction` |
| --- | --- | --- | ---: | --- | --- |
| 매우높음 | 최고위험 · 평균 대비 약 5~9배 | 0~0.005 | 0.047 | 4.7~9.3 | `true` |
| 높음 | 고위험 · 평균 대비 약 4~11배 | 0.005~0.02 | 0.221 | 4.4~11 | `true` |
| 다소높음 | 관찰 필요 · 평균 대비 약 3~4배 | 0.02~0.1 | 0.419 | 2.7~4.2 | `true` |
| 일반 | 특이신호 없음 | 0.1~1 | — | 1~1 | `true` |
| 정보부족 | 데이터 부족으로 평가 보류 | — | — | — | **`false`** |
| 이미공개 | 이미 명단공개된 곳 | — | — | — | **`false`** |

`lift` 는 화면에 `lift_low ~ lift_high` **범위로 표기**한다(DB 주석 지시).
`lift_low` = pooled 전체 회차 기준(보수적), `lift_high` = recent 2023~2026 배포대상 기준(낙관적).

### 4.2 `is_prediction = false` 의 의미

> false 면 예측이 아니라 사실·상태다(이미공개·정보부족). **위험 예측과 섞어 표시하지 말 것.**

`정보부족`과 `이미공개`는 모델 출력이 아니다. 나머지 4개와 같은 시각 언어(색·정렬·아이콘)를
쓰면 안 된다. 등급 정렬에도 포함하지 않는다.

### 4.3 백분위 모집단

DB 주석:

> 백분위 모집단은 batch 안에서 `risk_full IS NOT NULL AND 체불배제 = false` 인 행만.

즉 `정보부족`(risk_full NULL)과 `이미공개`(체불배제 true)는 백분위 계산에서 **제외**된 뒤
사후에 라벨만 붙는다. "상위 0.5%"는 전체 553,598곳 기준이 아니다.

### 4.4 실제 분포 (2026-08-29 실측, 최신 배치)

| tier | 사업장 수 | 비율 |
| --- | ---: | ---: |
| 일반 | 453,377 | 81.9% |
| **정보부족** | **49,703** | **9.0%** |
| 다소높음 | 40,301 | 7.3% |
| 높음 | 7,557 | 1.4% |
| 매우높음 | 2,519 | 0.5% |
| 이미공개 | 141 | 0.03% |
| **합계** | **553,598** | `batches.n_scored` 와 일치 |

**정보부족이 9%(49,703곳)** 다. 이를 `일반`과 같이 취급하면 5만 곳을 "특이신호 없음"으로
잘못 표시하게 된다(POL-05 위반).

### 4.5 `queue_priority` — 또 다른 척도

`inspector_queue.queue_priority` 는 큐 top 3,000 **안에서의 순위 기반**이다.

| 값 | rank 범위 (2026-08-29 실측) | 건수 |
| --- | --- | ---: |
| 긴급 | `1 ~ 100` | 100 |
| 우선 | `101 ~ 500` | 400 |
| 주의 | `501 ~ 1500` | 1,000 |
| 관찰 | `1501 ~ 3000` | 1,500 |

> ⚠️ **DB 주석이 틀렸다.** `0006_risk_tier.sql` 의 `COMMENT ON` 은
> `긴급(rank<100)/우선(<500)/주의(<1500)` 으로 적고 있으나 실제 데이터는 경계를 포함한다
> (`rank <= 100`). 위 표가 실측 기준이며, **주석 수정이 별도로 필요하다**(11.3절).

DB 주석: **`scored_active.risk_tier` 와 다른 척도다 — 같은 단어로 부르지 말 것.**

큐 정렬은 `rank` 로 한다. `queue_priority` 나 `risk_tier` 로 정렬하지 않는다(`v_current_queue` 주석).

> ✅ **쿼터는 전 배치에서 동일하다** [실측 2026-09-09 · 7개 배치 21,000행].
> 100 / 400 / 1,000 / 1,500 이 예외 없이 반복되며 5번째 값은 없다.

#### 4.5.1 큐의 나머지 두 컬럼

`inspector_queue` 에는 아래 두 boolean 도 있다. **둘 다 감독관 전용이다.**

| 컬럼 | CSV 이름 | 뜻 |
| --- | --- | --- |
| `door1_체납이력` | `door1_체납이력` | 4대보험 체납 공개명단 이력 유무. `scored_active.door1_ever` 와 같은 사실을 큐에 복제한 값 |
| `이미_임금체불공개` | `이미_임금체불공개` | 임금체불 공개명단에 이미 오른 곳인지. `safe_recommendation.체불배제` 와 같은 사실 |

**둘 다 모델 예측이 아니라 공표된 사실이다.** 화면에서 예측 신호와 섞지 않는다.
사용자 화면에는 노출하지 않는다(3.3절).

### 4.6 SHAP 사유 — `inspector_queue.reasons`

> SHAP 상위 3피처의 **이름**. ⚠️ **위험큐 상위 3,000곳에만 존재한다.**
> 나머지 사업장에는 위험사유가 없으며 **지어내면 안 된다**.

배열 길이는 **항상 정확히 3**이고(min=max=3), 큐 행 전원이 값을 갖는다
(21,000 / 21,000) **[실측 2026-08-31]**.

### ⚠️ 한글 라벨과 영문 원본 피처명이 섞여 있다

**절반 이상이 한글이 아니다.** 화면에 그대로 흘리면 **영문 변수명이 사용자에게 노출된다.**

| | 최신 배치(3,000행) | 전체(21,000행) |
| --- | ---: | ---: |
| distinct 피처 | **17종** | **20종** |
| 한글 라벨 | 9종 | 9종 |
| **영문 원본명** | **8종** | **11종** |

#### 전체 목록 **[실측 2026-08-31 · batch 7]**

| 한글 라벨 (9종) | 최신 | 전체 |
| --- | ---: | ---: |
| 체납액 | 2,137 | 15,200 |
| 4대보험 체납이력 | 1,166 | 7,720 |
| 고지금액 변동 | 983 | 7,288 |
| 지역 | 669 | 4,724 |
| 업력 | 108 | 747 |
| 급여수준 | 13 | 112 |
| 급여삭감 | 10 | 44 |
| 고지금액 급락 | 8 | 59 |
| 인원변동성 | 3 | 38 |

| 영문 원본명 (11종) | 최신 | 전체 | **한글 라벨** | 표시 |
| --- | ---: | ---: | --- | :-: |
| `door1_maxmonths` | 1,529 | 10,679 | 체납 지속기간 | 🟢 |
| `imputed_months_count` | 975 | 6,475 | (데이터 결측 개월수) | 🔴 |
| `imputed_ratio` | 834 | 5,523 | (데이터 결측 비율) | 🔴 |
| `industry_category` | 446 | 3,387 | 업종 | 🟡 |
| `replacement_avg_12m` | 105 | 875 | 인력 대체율 | 🟢 |
| `turnover_avg_12m` | 7 | 75 | 평균 퇴사율 | 🟢 |
| `replacement_min_12m` | 5 | 40 | 최저 인력 대체율 | 🟢 |
| `turnover_std_12m` | 2 | 7 | 퇴사율 변동 | 🟢 |
| `log_emp_count` | — | 5 | 사업장 규모 | 🟡 |
| `salary_change_12m` | — | 1 | 급여 12개월 변화 | 🟢 |
| `nf_emp_slope` | — | 1 | 인원 추세 | 🟢 |

🟢 위험 행태(사유 적합) · 🟡 사업장 속성(참고 정보) · 🔴 데이터 신뢰도(**사유 부적합**)

합계 검증: 최신 9,000 = 3,000 × 3 · 전체 63,000 = 21,000 × 3 ✅
`—` 는 최신 배치에 등장하지 않고 과거 배치에만 있는 피처다.

### 표시 규칙

1. **영문 원본명을 화면에 그대로 노출하지 않는다.** 사용자가 읽을 수 없는 변수명이다.
   39종 전체 라벨은 아래 사전에 있다
2. **🔴 2종은 사유 목록에서 제외한다** — `imputed_ratio`·`imputed_months_count`.
   모델 점수에는 정당히 기여하지만(부실 사업장은 신고를 멈춰 결측이 는다)
   화면에 "결측 보정 비율 높음"으로 뜨면 **사업장 위험이 아니라 데이터 사정**으로 읽힌다
3. **🟡 3종은 「참고 속성」으로 분리한다** — `industry_category`·`log_emp_count`·`sido_code`.
   사업장 속성이지 행태가 아니다. "건설업이라 위험" 식 프로파일링 인상을 피한다
4. 사전에 없는 피처명이 나오면 **`사유 확인 필요`** 로 표기한다. **의미를 지어내지 않는다**
   (모델이 고정돼 있어 39종 밖은 나오지 않는다. `model_version` 이 바뀌면 사전을 갱신한다)
5. `shap_value` **원값**은 POL-03에 따라 사용자·감독관 모두에게 노출하지 않는다.
   노출하는 것은 피처 **이름**뿐이다
6. 사유는 **감독관 화면 전용**이다. 구직자 화면에는 넣지 않는다

### 4.6.1 피처 사전 — 39종 전체

모델 담당 회신(2026-09-03) 기준. 대응 모델 **`model_version = door1-voting-39f-v1`**.
산출 코드(`scoring/features_shared.py` · `score_active_full.py` · `build_door1.py`)로 확인된 값이며
**추측이 아니다.**

> **모델이 고정돼 있어 SHAP 사유는 이 39종 안에서만 나온다.**
> 지금까지 20종만 등장했으나 나머지 19종도 언젠가 상위 3에 들 수 있다.
> 새 피처는 **재학습(= `model_version` 변경) 시에만** 생긴다.

**검증** — 39종이 `public.scored_active` 의 피처 컬럼과 **정확히 일치**한다 **[실측 2026-09-03]**.
운영 DB 54컬럼 = 비피처 15 + **피처 39**.

#### 🔴 값 읽는 법 — 단위·스케일·관측창 **[모델 담당 회신 2026-09-09 · 실측 대조 완료]**

값만 보고 뜻을 짐작하면 틀린다. 아래 다섯 가지를 먼저 읽는다.

| # | 항목 | 내용 |
| ---: | --- | --- |
| 1 | **금액 단위가 둘로 다르다** | `door1_maxamt` = **만원** · `salary_*` = **원/월** |
| 2 | **변화율은 소수 비율** | `0.15` = 15%. ×100 아님. **상한이 없다**(`emp_change_12m` 최대 289.4 실측) |
| 3 | **컬럼명 `_12m` 은 명목이다** | 실제 창은 `as_of_date` 에서 끝나는 약 1년 구간 (§6). 12를 뜻하지 않는다 |
| 4 | **로그는 `log1p`** | `log_emp_count` = `ln(가입자수 + 1)`, 밑 e |
| 5 | **결측 계열 분모는 12** | `imputed_ratio` = 보정 개월 ÷ **12** [역산 확인 11.98~12.05] |

> **급여 5종은 실지급액이 아니다.** 고지금액 ÷ 인원 ÷ 0.09 역산 추정치다.
> 화면·상담에서 "월급"으로 부르면 안 된다.

> 🟡 **달력 창은 13개월이 맞다. 다만 계수기는 12에서 멈춘다.**
>
> 창 길이 `[t-18, t-6]` **13개월**은 **독립된 세 출처가 일치**한다.
> ```
> db/migrations/0003_target_month.sql:4    [t-18 ── 관측창 13개월 ── t-6]
> docs/mlops/ml-db-data-contract.md §6.2   as_of = 관측창 종료월 · target = as_of + 6개월
> 모델 담당 회신 2026-09-09 (산출 코드)     관측창 = [t-18, t-6]
> ```
>
> 그런데 **월 단위 계수기 3종이 전부 12에서 멈춘다** [실측 2026-09-09 · 전체 7배치 385만 행].
> ```
> n_months                1 ~ 12     (13은 0건)
> zero_emp_months         0 ~ 12     (13은 0건)
> salary_drop_consecutive 0 ~ 12     (13은 0건)
> imputed_ratio 의 분모        12
> ```
>
> **두 사실은 모순이 아니라 서로 다른 층위다.** 달력 구간은 13개 시점이고,
> 그 위에서 세는 개월 수는 12가 상한이다. 둘의 관계는 **아직 확인되지 않았다**(§11.6).
>
> **주석·문서에 「13개월」만 단독으로 쓰지 않는다.** 읽는 사람이 `n_months` 를 조회하면
> 12까지만 나오기 때문이다. 쓸 때는 계수기 상한을 함께 적는다.

#### door1 (4대보험 체납) — 7종

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `door1_ever` | 4대보험 체납이력 | 체납 공개명단 이력 유무 | 🟢 |
| `door1_maxamt` | 체납액 | 최대 체납 금액. **단위 만원** (0 제외 최솟값이 1,000 = 공개 기준선) | 🟢 |
| `door1_maxmonths` | 체납 지속기간 | 체납이 가장 오래 지속된 개월 수 | 🟢 |
| `door1_n_insu` | 체납 보험 종수 | 체납한 보험 종류 수 | 🟢 |
| `door1_health` | 건강보험 체납 | 건강보험료 체납 여부 | 🟢 |
| `door1_pension` | 국민연금 체납 | 국민연금 보험료 체납 여부 | 🟢 |
| `door1_labor` | 고용·산재 체납 | 고용·산재보험료 체납 여부 | 🟢 |

#### 이직 · 인력 대체 — 8종

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `turnover_avg_12m` | 평균 퇴사율 | 퇴사율(상실/가입자) 1년 평균 | 🟢 |
| `turnover_avg_3m` | 최근 퇴사율 | 최근 3개월 평균 퇴사율 | 🟢 |
| `turnover_max_12m` | 최고 퇴사율 | 1년 중 최고 퇴사율 | 🟢 |
| `turnover_std_12m` | 퇴사율 변동 | 퇴사율 표준편차 | 🟢 |
| `turnover_momentum` | 퇴사율 가속 | 최근 3개월 ÷ 그 이전 평균 | 🟢 |
| `replacement_avg_12m` | 인력 대체율 | (신규+1)/(상실+1) 1년 평균. **1 미만 = 나간 자리를 안 채움** | 🟢 |
| `replacement_avg_3m` | 최근 인력 대체율 | 최근 3개월 대체율 | 🟢 |
| `replacement_min_12m` | 최저 인력 대체율 | 1년 중 대체율 최솟값 | 🟢 |

#### 인원 규모 · 변화 — 6종

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `emp_change_3m` | 3개월 인원변화 | 3개월 전 대비 가입자수 변화율 | 🟢 |
| `emp_change_6m` | 6개월 인원변화 | 6개월 전 대비 변화율 | 🟢 |
| `emp_change_12m` | 12개월 인원변화 | 1년 전 대비 변화율 | 🟢 |
| `emp_volatility` | 인원변동성 | 가입자수 변동계수 | 🟢 |
| `zero_emp_months` | 인원 0 개월수 | 관측창 중 가입자수 0인 개월 수 | 🟢 |
| `log_emp_count` | 사업장 규모 | `ln(가입자수 + 1)` = `log1p`. 실측 0 ~ 11.74 | 🟡 |

#### 급여 — 5종

> 급여는 **고지금액 ÷ 인원 ÷ 0.09 역산 추정치**다. 실제 지급액이 아니다.

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `salary_avg_12m` | 급여수준 | 1인당 추정 월급여의 관측창 평균. **단위 원/월** | 🟢 |
| `salary_last` | 최근 급여 | 마지막 달 1인당 추정 급여. **단위 원/월** | 🟢 |
| `salary_change_6m` | 급여삭감 | 6개월 전 대비 변화율(음수=삭감) | 🟢 |
| `salary_change_12m` | 급여 12개월 변화 | 1년 전 대비 변화율 | 🟢 |
| `salary_drop_consecutive` | 연속 급여하락 | 연속 하락한 최장 개월 수 | 🟢 |

#### 고지금액 · 납부 행태 — 7종

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `nf_bill_last_ratio` | 최근 고지금액 수준 | 마지막 달 고지금액 ÷ 관측창 평균. 배수 | 🟢 |
| `nf_bill_maxdrop` | 고지금액 급락 | 전월 대비 최대 하락 **비율**(0.3 = 30%). 금액 아님 | 🟢 |
| `nf_bill_cv` | 고지금액 변동 | 고지금액 변동계수 | 🟢 |
| `nf_pc_slope` | 1인당 납부액 추세 | 1인당 고지금액 추세 기울기 | 🟢 |
| `nf_pay_divergence` | 인원-납부 괴리 | 인원증가율 − 1인당납부증가율 | 🟢 |
| `nf_emp_slope` | 인원 추세 | 가입자수 추세 기울기(음수=감소) | 🟢 |
| `nf_drawdown` | 정점대비 인원낙폭 | 최대 인원 대비 마지막 인원 낙폭 | 🟢 |

#### 업력 · 속성 · 데이터 신뢰도 — 6종

| 피처 | 한글 라벨 | 뜻 | 표시 |
| --- | --- | --- | :-: |
| `firm_age_months` | 업력 | 사업장 성립~현재 개월 수 | 🟢 |
| `sido_code` | 지역 | 소재 시도 | 🟡 |
| `industry_category` | 업종 | 업종 대분류(KSIC 매핑) | 🟡 |
| `has_missing_recent_3m` | 최근 신고 누락 | 최근 3개월 중 원자료 결측(**신고 중단 정황**) | 🟢 |
| `imputed_months_count` | (데이터 결측 개월수) | 보정으로 채운 개월 수 | 🔴 |
| `imputed_ratio` | (데이터 결측 비율) | 보정 개월 수 **÷ 12** | 🔴 |

> **결측 계열 3종의 취급이 갈린다.** `has_missing_recent_3m` 는 **"신고를 멈췄다"** 는
> 사업장 행태라 사유로 적합하다. `imputed_*` 2종은 **데이터 사정**이라 부적합하다.

### 4.6.2 남은 결정 — 정보설계 담당

| 결정할 것 | 내용 |
| --- | --- |
| 🔴 2종 처리 | 사유 목록에서 빼되 "데이터 신뢰도 낮음" 캐비앗으로 쓸지 |
| 🟡 3종 배치 | 「참고 속성」 절을 따로 둘지, 우선순위만 낮출지 |
| 업종 표기 톤 | `건설업` 그대로 vs `고위험 업종군` 으로 완화 |

`sido_code`(지역)는 **이미 「지역」으로 노출 중**이다. 업종도 같은 톤이면 일관된다.

## 5. `risk_full` 을 확률로 읽지 않는다

DB 주석 원문(`0003_target_month.sql`):

> 위험점수 0~1. ⚠️ **확률이 아니다** — 양성:음성 1:1 다운샘플링으로 학습해서 "절반이 위험한
> 세상" 기준이다. 실제 기저율은 약 0.026%(146/552,500, 약 1/3,784)라 **약 1,900배 차이**가 난다.
> "체불 확률 N%" 로 말하지 말고 **순위·분위·등급으로만** 해석한다.
> NULL(약 9.2%)은 관측창 커버리지 부족으로 채점 불가이며 **0과 다르다**.
> *— 2026-08-29 실측 8.98%(49,711 / 553,598). 주석의 9.2%는 다른 배치 기준으로 보인다.*
> 예측 대상은 `batches.target_month` 시점의 명단공개다.

`risk_calibrated`:

> 캘리브레이션된 확률을 넣을 자리. **현재 전부 NULL** — 모델 문서상 캘리브레이션은 미완이며,
> 양성이 희소해 신뢰구간이 넓다. 준비되기 전까지 확률 표현을 쓰지 말 것.

**이 두 주석이 이 계약의 핵심 근거다.** `risk_calibrated` 가 비어 있는 것은 결함이 아니라
"아직 확률로 말할 수 없다"는 상태의 표시다. 채워질 때까지 확률 표현은 금지된다.

## 6. 기준일 3종

혼동하면 POL-06 위반이다.

| 필드 | 의미 | DB 주석 |
| --- | --- | --- |
| `batches.as_of_date` | 데이터 기준월 = 관측창의 끝(t-6) | NULL 이면 미확정이므로 **"언제 기준"이라고 말하면 안 된다**. ✅ **현재 NULL 0건** [실측 2026-09-09] |
| `batches.target_month` | 예측 대상월(t) = `as_of_date` + 6개월 | risk_full 은 "이 달에 명단공개될 위험"이며 **현재 상태가 아니다** |
| `official_listing.as_of` | **명단 자체의 공표일** | 모델 배치일로 대신하지 않는다. 없으면 `null` |

최신 배치 기준 `as_of_date=2026-06-01`, `target_month=2026-12-01`.

### 🔴 피처는 예측 대상월보다 **6개월 이전** 자료다

두 문서가 독립적으로 같은 구조를 말한다.

```
docs/mlops/ml-db-data-contract.md §6.2   as_of = 관측창 종료월 · target_month = as_of + 6개월
모델 담당 회신 (산출 코드 근거)           관측창 = [t-18, t-6]

t = target_month  →  t-6 = as_of = 관측창 종료월
실측: as_of 2026-06-01 · target_month 2026-12-01 (6개월 차)
```

**즉 모든 피처는 `as_of_date` 까지의 자료로 만들어졌다.**
화면에 `target_month`(2026-12)를 기준일처럼 쓰면 **6개월 앞선 자료로 오인**하게 만든다.

> 창의 **길이**는 미확정이다 (§4.6.1 · §11.6). 종료 시점이 `as_of_date` 인 것만 확정이다.

**화면에 "2026-06 기준"이라고 쓰면 관측 시점을 뜻한다.** 예측 대상은 6개월 뒤다.
둘 중 무엇을 표시할지는 화면별로 지정해야 한다 — **미확정 항목**(화면 담당·정보설계 담당과 협의).

### 6.1 API 응답의 기준일 필드 — POL-11 대조 **[실측 2026-08-31]**

POL-11 은 `data_as_of` · `generated_at` · `valid_until` · `freshness` 표시를 요구한다.

| 필드 | 실제 값 | 출처 | POL-11 |
| --- | --- | --- | :-: |
| `data_as_of` | `"2026-06-01"` | `batches.as_of_date` | ✅ |
| `target_month` | `"2026-12-01"` | `batches.target_month` | — |
| `generated_at` | `"2026-08-14T15:02:34.715Z"` | `batches.ingested_at` | ✅ |
| `valid_until` | **`null`** | 하드코딩 | 🔴 |
| `freshness` | **`"unknown"`** | 하드코딩 | 🔴 |

> 🔴 **`valid_until` 이 없어 POL-11 의 유효기간 요건을 충족할 수 없다.** 11.5절 참조.

### 6.2 `sources[]` 필드 규정 **[실측 2026-08-31]**

응답에 **항상 2건**이 들어간다 — `wage` 1건, `safety` 1건. 6개 판정 상태 전부 동일했다.

| 필드 | 필수 | 예시 | 의미 |
| --- | :-: | --- | --- |
| `name` | ✅ | `국민연금 사업장 자료 및 ML 공개 판정` | **데이터명**. POL-11 의 "문서명 또는 데이터명" |
| `category` | ✅ | `wage` / `safety` | 카드 구분. **두 카드를 합산하지 않는다**(POL-06) |
| `organization` | ✅ | `돈워리 임금체불 데이터 파이프라인` | ⚠️ 아래 주의 |
| `as_of` | ✅ | `2026-06-01` (wage) · `2026-08-14T15:02:34.715Z` (safety) | 기준일. **형식이 두 가지다** |
| `document_id` | ✅ | `door1-voting-39f-v1:batch-7` | 모델·배치 식별. 재현 근거 |

> ⚠️ **`organization` 은 원출처 기관이 아니다.** `돈워리 … 데이터 파이프라인`,
> 즉 **우리 쪽 산출 주체**다. 화면에 "출처: 돈워리 …" 로 그대로 쓰면
> 사용자가 원자료 출처(국민연금 사업장 자료 등)로 오해할 수 있다.
> **원출처는 `name` 에 들어 있다.** 화면 문구는 `name` 을 우선한다.

> ⚠️ **`as_of` 형식이 통일돼 있지 않다.** wage 는 날짜(`YYYY-MM-DD`),
> safety 는 ISO 타임스탬프다. 같은 목록에 나란히 표시할 때 **날짜로 정규화**한다.

## 7. 식별키 주의

| 컬럼 | DB 주석 요지 |
| --- | --- |
| `firms.firm_id` | `sha1(사업장명\|사업자번호)[:16]`. ⚠️ **불변이 아니다** — 사업장명이 바뀌면 달라진다. 변동률은 **[출처 미확인]** (아래) |
| `firms.biz_no` | 마스킹 6자리. ⚠️ **비고유** — 한 번호가 **최대 950곳**에 재사용된다(2026-08-29 실측). 단독 식별 불가 |
| `firms.corp_key` | 표기 변형 흡수용 정규화 키. 같은 법인 묶기용이며 **식별키가 아니다** |

`biz_no` 단독 조인 금지. `firm_id` 는 서버에서 재검증한다(POL-07).

본선 제안서 3.1.5가 **"안정 사업장 식별키 확보"** 를 보완 과제로 지목한 이유가 이것이다.

### 7.1 🔴 「월 약 0.24%」는 근거를 찾지 못했다

이 수치는 저장소 **5곳**에 인용돼 있다 — 이 문서, `db/schema.ts`, `db/README.md`,
`migration 0002`, `docs/mlops/ml-db-data-contract.md`. **산출 방법을 적은 곳이 없다.**

두 가지 방법으로 재현을 시도했다 **[실측 2026-09-09 · 인접 배치 6쌍 전부]**.

| 배치→다음 | 사업장 | 다음에 없음 | 전체 이탈 | 같은 `corp_key` 잔존 | 이름변경 추정 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 1→2 | 553,466 | 23,731 | 4.288% | 51 | 0.0092% |
| 2→3 | 546,370 | 15,998 | 2.928% | 53 | 0.0097% |
| 3→4 | 549,377 | 17,699 | 3.222% | 53 | 0.0096% |
| 4→5 | 547,944 | 16,409 | 2.995% | 45 | 0.0082% |
| 5→6 | 552,500 | 16,145 | 2.922% | 53 | 0.0096% |
| 6→7 | 552,593 | 16,148 | 2.922% | 39 | 0.0071% |

```
상한 (모든 이탈)                    2.9 ~ 4.3% / 월
하한 (corp_key 로 잡힌 이름변경)    0.007 ~ 0.010% / 월
인용된 값                           0.24% / 월        ← 어느 쪽과도 맞지 않음
```

**측정 한계**
- **상한**에는 폐업·국민연금 이탈 등 **키 문제가 아닌 이탈**이 섞여 있다
- **하한**은 `corp_key` 가 `㈜·(주)·주식회사` 표기만 정규화하므로
  **상호를 실제로 바꾼 경우를 잡지 못한다**
- 참값은 두 값 사이다. 0.24%가 그 구간 안에 있기는 하다

**따라서 이 문서는 0.24%를 사실로 쓰지 않는다.** 산출 방법을 확인하면 근거와 함께 되살리고,
확인되지 않으면 위 실측 구간으로 대체한다. 모델 담당에 문의했다.

> 참고: 7개 배치 **전부**에 존재하는 `firm_id` 는 639,137개 중 **478,080개(74.80%)** 다.

## 8. 미연결 3개 지표

POL-05-1이 정의한다. 사용자 위험카드 4개 항목 중 실연동은 **1개**뿐이다.

| 항목 | 상태 |
| --- | --- |
| 체불사업주 명단 | ✅ 실연동 (`official_listing`) |
| 이직률 (12개월) | ❌ `확인할 수 없음` |
| 고용 추이 | ❌ `확인할 수 없음` |
| 데이터 충실도 | ❌ `확인할 수 없음` |

**`scored_active` 에 이름이 비슷한 컬럼이 실제로 존재한다**(`turnover_avg_12m`,
`emp_change_12m`, `imputed_ratio` 등). 쓰면 안 된다. 모델 학습용 피처이지 출처·기준일이
검증된 공개 지표가 아니다. 내부 피처를 사용자용 값으로 변환하는 것은 POL-05-1 위반이다.

연동에 필요한 것(**미확정 — 별도 과제**):

- 출처 기관·API·공표 주기
- 기준일 정의
- `firms` 와의 매칭 키와 실패 시 상태
- `no_data`(결과 없음)와 `unavailable`(공급자 장애) 구분 기준

## 9. 조회 규칙

### 9.1 ⚠️ 주석의 지시와 현재 구현이 다르다

DB 주석은 뷰 사용을 지시하지만, **현재 Web 코드는 원본 테이블을 직접 조회한다.**

| | DB 주석의 지시 | 현재 구현 (2026-08-29 확인) |
| --- | --- | --- |
| 임금 판정 | `v_current_safe` | `public.safe_recommendation` + `latest_batch` CTE |
| 채점 | `v_current_scored` | `public.scored_active` + `latest_batch` CTE |
| 배치 | `v_current_batch` | `public.batches` + `LATEST_BATCH_ORDER_SQL` |
| 산업안전 | — | `industrial_safety.v_llm_firm_safety_context` (뷰 사용) |

근거: `product/src/adapters/real/MlRiskProvider.ts` 253~298행.
`grep -rn "v_current_" product/src --include=*.ts` 결과 0건(테스트 제외).

최신 배치 선택은 뷰 대신 `product/src/server/latestBatchSql.ts` 의 `LATEST_BATCH_ORDER_SQL`
이 담당하며, 정렬 규칙(`as_of_date DESC, ingested_at DESC, id DESC`)은 `v_current_batch` 와 동일하다.
**결과는 같지만 경로가 다르다.**

코드를 뷰 기반으로 바꿀지는 별도 과제다(11.4절). 그 전까지 이 문서는 **현재 구현을 기술한다.**

### 9.2 뷰의 설계 의도

아래는 DB 주석이 정의한 각 뷰의 용도다. 코드가 뷰를 쓰게 될 때 이 규칙을 따른다.

| 뷰 | 용도 | 주의 |
| --- | --- | --- |
| `v_current_batch` | 최신 배치 1행 | `as_of_date` 기준. 같은 기준일은 `ingested_at`, `id` 내림차순. **`id` 만으로 고르지 않는다**(백필 때문) |
| `v_current_scored` | 최신 배치 채점 | 화면·챗봇은 원본 대신 이걸 쓴다 — `batch_id` 를 빠뜨려도 안전 |
| `v_current_queue` | 최신 배치 감독관 큐 | 정렬은 `rank` 로 |
| `v_current_safe` | 최신 배치 구직자 판정 | **구직자 화면은 위험등급 대신 이 판정만** |
| `v_risk_history` | 사업장 월간 추이 | 과거 달에 없던 사업장은 **행이 없다**(폐업·신규 = 정상). **선을 잇지 말고 끊어서 그릴 것** |

`v_risk_history` 추가 주의(DB 주석): `체불배제`·`체납배제` 플래그는 **점-인-타임이 아니라
현재 상태**다. 과거 시점 그래프에 현재 플래그를 소급 적용하지 않는다.

`wg_bot` 계정은 `users`·`posts`·`comments`·`reviews` 원본에 접근 권한이 없다.
커뮤니티는 `v_posts`·`v_comments`·`v_reviews`(신원 제거)로만 읽는다.

## 10. 표현 정책 참조

중복 정의하지 않는다. `product/docs/service-policy.md` 를 따른다.

| 상황 | 정책 |
| --- | --- |
| 원시 수치 노출 | POL-03 |
| `normal` 해석 | POL-04 |
| `unknown` 해석 | POL-05 |
| 미연결 지표 | POL-05-1 |
| 명단 등재 사실 분리 | POL-06 |
| `company_id` 조회 | POL-07 |
| 기준일·출처 표시 | POL-11 |

## 11. 알려진 문제와 요청 항목

이 계약 범위 밖이지만 기록해 둔다.

### 11.1 운영 DB에 스키마 주석이 없다

이 문서의 근거가 되는 `COMMENT ON` 49건이 **저장소 migration에만 있고 운영 DB에는 0건**이다.
Path B release dump가 `--no-comments` 로 생성돼 복원 과정에서 전부 제외됐다
(`path-b-release.metadata.json` 의 `"no_comments": true`).

- 영향: 스키마를 직접 조회하는 사람·AI 에이전트가 경고를 읽지 못한다
- 조치: migration의 `COMMENT ON` 구문만 재실행하면 복원된다(데이터 변경 없음)
- 담당: `db/migrations/**` 사용자 DB 담당 · 운영 반영 인프라 담당

### 11.2 테스트 블록리스트에 `risk_tier` 가 없다

사용자 응답에 내부 필드가 섞이는지 검사하는 테스트가 두 곳 있으나,
**`risk_tier` 는 어느 목록에도 없다.**

```javascript
// product/src/services/policy.test.ts:95
["raw_probability", "percentile", "shap_value", "internal_score", "model_threshold", ...]

// product/src/services/integrationContract.test.ts:51
/risk_full|probability|percentile|shap/i
```

숫자 원점수는 막지만 **등급 라벨은 통과한다.** 사용자 입장에서 "매우높음"은 `0.9904` 보다
이해하기 쉬우므로 오히려 더 위험하다. 2절 규칙을 코드로 강제하려면 두 목록에
`risk_tier` 를 추가해야 한다.

- 담당: `product/src/**` — 공통 파일 규칙상 인프라 담당 검토 필요

### 11.3 DB 주석에 사실 오류가 있다

이 계약의 1차 근거인 `COMMENT ON` 중 **검증 가능한 수치 6건을 전수 대조한 결과 2건이 틀렸다.**

| 주석 위치 | 주석 값 | 실측 (2026-08-29) | |
| --- | --- | --- | --- |
| `0006_risk_tier.sql` `queue_priority` | `긴급(rank<100)` | `rank <= 100` | ❌ |
| `0002_bot_views.sql` `firms.biz_no` | 최대 **788곳** 재사용 | **950곳** | ❌ |
| `0003_target_month.sql` `risk_full` | NULL 약 9.2% | 8.98% | ⚠️ 근사 |
| `0002_bot_views.sql` `inspector_queue.reasons` | 상위 3,000곳에만 | 3,000 / 3,000 | ✅ |
| `0006_risk_tier.sql` 백분위 모집단 | `risk_full NOT NULL AND 체불배제=false` | 503,754 = 등급 4종 합 | ✅ |
| `0003_target_month.sql` `risk_calibrated` | 전부 NULL | 0 / 553,598 | ✅ |

**이 문서는 실측값을 쓴다.** 다만 주석을 고치지 않으면 다음에 스키마를 조회하는 사람·에이전트가
같은 오류를 반복한다.

- 조치: `db/migrations` 의 해당 `COMMENT ON` 수정
- 담당: 사용자 DB 담당 (`db/migrations/**`)
- 함께: 11.1의 주석 재적용 작업과 묶어 처리하면 효율적이다

### 11.4 조회 방식을 코드에 맞출지 뷰에 맞출지

9.1절 참고. DB 주석은 뷰 사용을 지시하나 코드는 원본 테이블을 직접 조회한다.

| 선택지 | 영향 |
| --- | --- |
| 문서를 코드에 맞춘다 (현재) | 주석의 설계 의도가 사문화된다 |
| 코드를 뷰로 바꾼다 | `product/src/**` 변경. 공통 파일 규칙상 인프라 담당 검토 필요 |

현재는 첫 번째를 택했다. 두 번째로 갈지는 결정이 필요하다.

### 11.5 `valid_until` 이 항상 `null` 이라 POL-11 을 지킬 수 없다

**[실측 2026-08-31]** `MlRiskProvider.ts:323-324` 가 두 값을 **하드코딩**한다.

```ts
valid_until: null,
freshness: "unknown",
```

`riskService.ts:11-12` 의 `getFreshnessFromValidUntil()` 은 `validUntil === null` 이면
`"unknown"` 을 돌려준다. 따라서 **freshness 가 `current`/`expired` 로 갈 수 없다.**

POL-11 의 요구를 구조적으로 수행할 수 없다.

> *"`valid_until` 이 지난 결과에는 '자료 갱신 필요' 또는 '유효기간이 지난 결과'를 표시한다."*
> *"오래된 결과를 최신 정보처럼 표현하지 않는다."*

배치가 6개월 주기(`as_of_date` → `target_month`)인데 **유효기간 판정이 없으면
오래된 배치를 최신처럼 보여주게 된다.**

| 결정할 것 | 후보 |
| --- | --- |
| `valid_until` 산출 기준 | `target_month` 말일? 다음 배치 예정일? 고정 기간? |
| 값이 없을 때 화면 처리 | 현재는 `freshness="unknown"`. 문구가 필요한지 |

- 담당: `product/src/**` — 공통 파일 규칙상 인프라 담당 검토 필요
- 성격: **정책 미충족**이지 계약 수치 오류는 아니다

### 11.6 미확정 항목

| 항목 | 결정 필요 | 관련 담당 |
| --- | --- | --- |
| `유보` 85.7% 편중의 화면 처리 | 시각 강조 수준 | 화면 담당·정보설계 담당 |
| 화면별 기준일 표시(`as_of_date` vs `target_month`) | 화면마다 지정 | 화면 담당·정보설계 담당 |
| `reasons` 영문 원본명 **11종** 라벨화 | **선행**: 저장소에 정의 없음 → 피처 사전 확보 | 모델 담당 → 정보설계 담당 |
| 미연결 3개 지표 데이터 소스 | 8절 4개 항목 | ML·DB 검토 담당(별도 과제) |
| **`valid_until` 산출 기준** | 11.5절 — POL-11 유효기간 요건 미충족 | 인프라 담당 검토 |
| `sources[].organization` 화면 표기 | 6.2절 — 원출처가 아니라 파이프라인명 | 화면 담당·정보설계 담당 |
| **관측창 길이 12 vs 13** | 회신은 13개월, 운영 DB 계수기는 12 (§4.6.1) | 모델 담당 |
| **`g5_업력3년` 기준일 고정** | 아래 11.7절 — 이름과 실제가 다름 | 모델 담당 |
| `nf_` 접두 약자 | 저장소·산출 코드 어디에도 정의 없음 [실측 2026-09-09] | 모델 담당 |
| **`유보_정보부족` 화면 문구** | 2.1절 — 감독관 `매우높음` 696곳·`높음` 2,673곳이 구직자에게 「자료 부족」으로만 보임. 「자료가 없다」와 「자료가 짧아 보류했다」를 구분할지 | **회의** |

### 11.7 🔴 `g5_업력3년` 이 이름과 다르다

긍정 지표 G5 는 「업력 3년」으로 불리지만 **기준일이 코드에 고정**돼 있다.
경계를 직접 재 보았다 **[실측 2026-09-09]**.

```
 g5_업력3년 | 업력 최소 | 업력 최대 |   건수
 false      |       0.0 |      37.0 |  77,311
 true       |      38.0 |     461.0 | 426,576
```

**경계가 38개월**이다. 굴러가는 3년이면 36이어야 한다.

```
배치 as_of 2026-06  −  코드 고정 기준일 2023-04  =  38개월
```

즉 현재 기준은 **3년 2개월**이며, **배치가 진행될수록 벌어진다.**
다음 배치(`as_of` 2026-07)면 39개월이 된다.

| 영향 | 내용 |
| --- | --- |
| 화면 | 「업력 3년 이상」으로 표기하면 사실과 다르다 |
| 판정 | G5 는 `n_green` 에 들어가므로 안정신호 조건(`n_green≥5`)에 영향 |
| 시간 | 배치마다 기준이 달라져 **배치 간 비교가 성립하지 않는다** |

**요청**: 기준일을 `as_of_date` 기준 상대값으로 바꿀 수 있는지. 담당 — 모델.
바꾸기 전까지 화면 표기는 「업력 약 3년 이상」으로 한다.

---

## 부록. 검증 방법

```bash
# 판정 분포
psql -c "select 판정, count(*) from v_current_safe group by 1 order by 2 desc;"

# 등급 분포
psql -c "select risk_tier, count(*) from v_current_scored group by 1 order by 2 desc;"

# 등급 해설표
psql -c "select * from risk_tier_meta order by sort_order;"

# 변환 코드
sed -n '55,135p' product/src/adapters/real/MlRiskProvider.ts
```

**코드가 바뀌면 이 문서도 함께 바뀌어야 한다.** 3절 표는
`MlRiskProvider.ts` 의 `VERDICT_META` 와 1:1 대응이다.
